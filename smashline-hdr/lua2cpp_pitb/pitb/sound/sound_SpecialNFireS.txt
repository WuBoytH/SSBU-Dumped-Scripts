#[acmd_script( agent = "pitb", script = "sound_specialnfires" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialnfires(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    frame(lua_state, 2.0);
    if is_excute(fighter) {
        STOP_SE(fighter, Hash40::new("se_pitb_special_n01"));
        PLAY_SE(fighter, Hash40::new("vc_pitb_special_n01"));
    }
    wait(lua_state, 4.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_pitb_special_n02"));
        SET_PLAY_INHIVIT(fighter, Hash40::new("se_pitb_special_n02"), 30);
    }
}