#[acmd_script( agent = "pitb", script = "game_specialairnfires", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairnfires(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        ArticleModule::shoot(boma, *FIGHTER_PITB_GENERATE_ARTICLE_BOWARROW, *ARTICLE_OPE_TARGET_ALL, false);
    }
}