#[acmd_script( agent = "mewtwo", script = "game_finalairend" , category = ACMD_GAME , low_priority)]
unsafe fn game_finalairend(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		WHOLE_HIT(fighter, *HIT_STATUS_INVINCIBLE);
	}
	frame(lua_state, 13.0);
	if is_excute(fighter) {
		WorkModule::on_flag(boma, *FIGHTER_MEWTWO_STATUS_FINAL_FLAG_FINAL_CHANGE);
		CANCEL_FILL_SCREEN(fighter, 0, 180);
	}
}