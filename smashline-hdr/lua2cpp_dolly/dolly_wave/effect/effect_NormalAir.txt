unsafe extern "C" fn effect_normalair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("dolly_wave_air"), Hash40::new("top"), 0, 0, -1, 0, 0, 0, 0.85, true);
        LAST_EFFECT_SET_RATE(agent, 1.4);
        EFFECT_FOLLOW(agent, Hash40::new("dolly_wave_air_flash"), Hash40::new("top"), 0, 0, -1, 0, 0, 0, 0.9, true);
        LAST_EFFECT_SET_RATE(agent, 1.4);
    }
}