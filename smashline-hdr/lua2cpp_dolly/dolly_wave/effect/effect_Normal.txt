#[acmd_script( agent = "dolly_wave", script = "effect_normal", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_normal(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("dolly_wave"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}