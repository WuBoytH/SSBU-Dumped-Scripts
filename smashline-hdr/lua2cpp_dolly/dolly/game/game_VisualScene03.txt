#[acmd_script( agent = "dolly", script = "game_visualscene03", category = ACMD_GAME, low_priority )]
unsafe fn game_visualscene03(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 11.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_DOLLY_STATUS_FINAL_SCENE03_WORK_FLAG_PLAY_MOTION_OPPONENT);
    }
    frame(lua_state, 20.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_DOLLY_STATUS_FINAL_SCENE03_WORK_FLAG_SET_MOTION_RATE_OPPONENT);
    }
}