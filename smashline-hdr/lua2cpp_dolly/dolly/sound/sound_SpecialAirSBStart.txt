#[acmd_script( agent = "dolly", script = "sound_specialairsbstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairsbstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if WorkModule::is_flag(boma, *FIGHTER_DOLLY_STATUS_SPECIAL_COMMON_WORK_FLAG_COMMAND) {
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_dolly_command_success"));
            PLAY_SE(agent, Hash40::new("se_dolly_special_sb01"));
        }
        else{
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_dolly_special_sb01"));
        }
    }
}
}