#[acmd_script( agent = "gekkouga_water", script = "sound_specialhil", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialhil(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_TAKEOUT_SE(agent, Hash40::new("se_common_c_water_m"));
    }
}