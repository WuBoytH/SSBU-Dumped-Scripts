#[acmd_script( agent = "gekkouga", script = "sound_specialhistopwall", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialhistopwall(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_gekkouga_special_h01"));
    }
}