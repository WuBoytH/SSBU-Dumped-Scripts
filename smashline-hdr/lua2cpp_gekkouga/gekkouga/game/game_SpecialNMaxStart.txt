#[acmd_script( agent = "gekkouga", script = "game_specialnmaxstart", category = ACMD_GAME, low_priority )]
unsafe fn game_specialnmaxstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 13.0);
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_GEKKOUGA_GENERATE_ARTICLE_SHURIKEN, false, -1);
    }
}