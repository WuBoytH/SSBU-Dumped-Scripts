#[acmd_script( agent = "gekkouga", script = "expression_scopewait", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_scopewait(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
    }
}