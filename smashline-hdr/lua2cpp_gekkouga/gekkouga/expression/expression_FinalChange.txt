#[acmd_script( agent = "gekkouga", script = "expression_finalchange", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_finalchange(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        START_INFO_FLASH_EYE(agent);
        WorkModule::on_flag(boma, *FIGHTER_INSTANCE_WORK_ID_FLAG_BACKSHIELD_INVISIBLE);
        ItemModule::set_have_item_visibility(boma, false, 0);
    }
    frame(lua_state, 20.0);
    if is_excute(agent) {
        ArticleModule::set_visibility_whole(boma, *FIGHTER_GEKKOUGA_GENERATE_ARTICLE_GEKKOUGAS, true, ArticleOperationTarget(*ARTICLE_OPE_TARGET_ALL));
    }
    frame(lua_state, 22.0);
    if is_excute(agent) {
        VisibilityModule::set_whole(boma, false);
    }
}