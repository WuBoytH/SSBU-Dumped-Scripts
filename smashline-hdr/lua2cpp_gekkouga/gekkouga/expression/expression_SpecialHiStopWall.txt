#[acmd_script( agent = "gekkouga", script = "expression_specialhistopwall", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_specialhistopwall(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        notify_event_msc_cmd(0x1f20a9d549, true);
        notify_event_msc_cmd(0x24772eddef, true);
    }
}