#[acmd_script( agent = "gekkouga_shuriken", script = "effect_maxshot", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_maxshot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("gekkouga_mizusyuriken_tail"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}