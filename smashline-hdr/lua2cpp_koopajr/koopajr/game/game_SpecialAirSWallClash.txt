unsafe extern "C" fn game_specialairswallclash(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        SET_SPEED_EX(agent, -1.25, 1.8);
    }
}