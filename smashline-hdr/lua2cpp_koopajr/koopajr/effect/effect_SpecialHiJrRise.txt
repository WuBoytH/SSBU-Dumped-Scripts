#[acmd_script( agent = "koopajr", script = "effect_specialhijrrise", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialhijrrise(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("koopajr_zibaku_line"), Hash40::new("rot"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}