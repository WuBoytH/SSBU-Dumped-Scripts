#[acmd_script( agent = "miifighter", script = "sound_specialhi3" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialhi3(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    frame(lua_state, 4.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_miifighter_special_c3_h01"));
    }
    frame(lua_state, 20.0);
    if is_excute(fighter) {
        PLAY_SEQUENCE(fighter, Hash40::new("seq_miifighter_rnd_special_c3_h01"));
    }
    frame(lua_state, 21.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_miifighter_special_c3_h02"));
    }
}