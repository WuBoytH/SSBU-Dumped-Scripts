#[acmd_script( agent = "miifighter", script = "sound_finalairstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finalairstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_miifighter_final02"));
        PLAY_SE(agent, Hash40::new("vc_mii_finalstart"));
    }
    frame(lua_state, 21.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_miifighter_swing_ll"));
    }
}