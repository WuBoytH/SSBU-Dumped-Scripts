#[acmd_script( agent = "miifighter", script = "sound_specialn3turn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialn3turn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 48.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_miifighter_special_c3_n02"));
        PLAY_SEQUENCE(agent, Hash40::new("seq_miifighter_rnd_special_c3_n01"));
    }
}