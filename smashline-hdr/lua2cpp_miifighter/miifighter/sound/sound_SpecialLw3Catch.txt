#[acmd_script( agent = "miifighter", script = "sound_speciallw3catch", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallw3catch(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_miifighter_special_c3_l02"));
    }
}