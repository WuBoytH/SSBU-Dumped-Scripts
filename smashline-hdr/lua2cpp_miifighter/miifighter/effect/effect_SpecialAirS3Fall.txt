#[acmd_script( agent = "miifighter", script = "effect_specialairs3fall", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairs3fall(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("miifighter_suplex_line"), Hash40::new("top"), 0, 12, 0, 0, 0, 0, 1, true);
    }
}