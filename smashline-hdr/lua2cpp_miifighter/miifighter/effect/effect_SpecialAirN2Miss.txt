#[acmd_script( agent = "miifighter", script = "effect_specialairn2miss", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairn2miss(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, 0x1aa2c81ef2, false, false);
    }
}