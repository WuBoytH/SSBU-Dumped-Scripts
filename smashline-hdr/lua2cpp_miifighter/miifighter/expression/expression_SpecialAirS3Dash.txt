#[acmd_script( agent = "miifighter", script = "expression_specialairs3dash", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairs3dash(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        ItemModule::set_have_item_visibility(boma, false);
    }
    frame(lua_state, 10.0);
    if is_excute(fighter) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_dash"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
    frame(lua_state, 48.0);
    if is_excute(fighter) {
        ItemModule::set_have_item_visibility(boma, true);
    }
}