#[acmd_script( agent = "miifighter", script = "expression_speciallw2start" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_speciallw2start(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_jump"), 0, false);
        ControlModule::set_rumble(boma);
    }
}