#[acmd_script( agent = "kamui_ryusensya", script = "sound_shotmax", category = ACMD_SOUND, low_priority )]
unsafe fn sound_shotmax(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_kamui_special_n04"));
    }
}