#[acmd_script( agent = "kamui", script = "expression_attacks4_cancel", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_attacks4_cancel(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        EFFECT_OFF_KIND(fighter, Hash40::new("kamui_transform_splash_end"), true, true);
        EFFECT_OFF_KIND(fighter, Hash40::new("sys_smash_flash"), true, true);
        STOP_SE(fighter, Hash40::new("se_kamui_horn_start"));
    }
}