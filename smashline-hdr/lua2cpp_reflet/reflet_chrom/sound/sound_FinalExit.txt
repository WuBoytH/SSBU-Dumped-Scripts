#[acmd_script( agent = "reflet_chrom", script = "sound_finalexit", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finalexit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_reflet_final17"));
    }
}