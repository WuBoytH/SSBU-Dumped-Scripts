#[acmd_script( agent = "reflet", script = "effect_finalfail", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalfail(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EffectModule::remove_screen(boma, Hash40::new("bg_reflet_final"), -1);
    }
}