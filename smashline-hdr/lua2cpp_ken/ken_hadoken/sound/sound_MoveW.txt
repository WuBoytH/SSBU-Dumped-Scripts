#[acmd_script( agent = "ken_hadoken", script = "sound_movew" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_movew(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		PLAY_SE(fighter, Hash40::new("se_ken_special_n04"));
	}
}