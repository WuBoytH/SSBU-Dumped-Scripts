#[acmd_script( agent = "pit", script = "game_specialairnstart", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairnstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_PIT_GENERATE_ARTICLE_BOWARROW, false, -1);
    }
}