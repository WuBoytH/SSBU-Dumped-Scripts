#[acmd_script( agent = "pit", script = "sound_slip", category = ACMD_SOUND, low_priority )]
unsafe fn sound_slip(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_slip_02"));
    }
    wait(lua_state, 17.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_falldown_03"));
    }
}