#[acmd_script( agent = "pit", script = "effect_finalride", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalride(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EffectModule::set_visible_kind(boma, Hash40::new("sys_final_aura"), false);
    }
}