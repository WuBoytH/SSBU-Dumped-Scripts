#[acmd_script( agent = "eflame_esword", script = "sound_back", category = ACMD_SOUND, low_priority )]
unsafe fn sound_back(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_eflame_special_s04"));
    }
}