#[acmd_script( agent = "eflame_esword", script = "effect_hop", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_hop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        AFTER_IMAGE_OFF(agent, 4);
    }
}