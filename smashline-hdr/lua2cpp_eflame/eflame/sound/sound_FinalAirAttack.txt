#[acmd_script( agent = "eflame", script = "sound_finalairattack", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finalairattack(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_eflame_final03"));
    }
    frame(lua_state, 120.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_eflame_final04"));
    }
}