#[acmd_script( agent = "nana", script = "sound_specialairhistart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairhistart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 11.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_popo_special_h01"));
    }
}