#[acmd_script( agent = "nana", script = "sound_attackairlw_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackairlw_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 9.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_popo_attackair_l01"));
    }
}