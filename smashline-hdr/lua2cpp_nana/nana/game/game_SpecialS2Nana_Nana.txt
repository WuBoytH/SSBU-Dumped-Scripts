#[acmd_script( agent = "nana", script = "game_specials2nana_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_specials2nana_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 60.0);
    if is_excute(agent) {
        REVERSE_LR(agent);
    }
}