unsafe extern "C" fn game_entryr(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_POPO_GENERATE_ARTICLE_CONDOR, true, -1);
        ArticleModule::change_motion(boma, *FIGHTER_POPO_GENERATE_ARTICLE_CONDOR, Hash40::new("entry_r"), false, -1.0);
    }
}