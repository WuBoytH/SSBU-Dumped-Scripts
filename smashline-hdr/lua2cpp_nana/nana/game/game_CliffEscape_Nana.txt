#[acmd_script( agent = "nana", script = "game_cliffescape_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_cliffescape_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    FT_MOTION_RATE(agent, 0.91);
}