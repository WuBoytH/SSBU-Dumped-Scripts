#[acmd_script( agent = "nana", script = "expression_finalair1r", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_finalair1r(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ItemModule::set_have_item_visibility(boma, false, 0);
        START_INFO_FLASH_EYE(agent);
    }
    frame(lua_state, 67.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_15_iceberg_sp"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
    frame(lua_state, 89.0);
    if is_excute(agent) {
        ModelModule::disable_gold_eye(boma);
    }
}