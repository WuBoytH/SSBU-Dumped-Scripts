#[acmd_script( agent = "nana", script = "expression_entryrnana_nana", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_entryrnana_nana(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 108.0);
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_L, 4);
    }
}