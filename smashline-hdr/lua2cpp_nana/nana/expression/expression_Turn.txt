#[acmd_script( agent = "nana", script = "expression_turn", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_turn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_L);
    }
    frame(lua_state, 8.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_POPO_STATUS_TURN_FLAG_REVERSE_FLIP_STATUS);
    }
}