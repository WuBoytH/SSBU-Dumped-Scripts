#[acmd_script( agent = "yoshi", script = "sound_squatb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squatb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 8.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_yoshi_crawing_right_b"));
    }
    frame(lua_state, 28.0);
    PLAY_STEP(agent, 0x176ad908e2);
}