#[acmd_script( agent = "yoshi_tamago", script = "sound_throwed", category = ACMD_SOUND, low_priority )]
unsafe fn sound_throwed(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_TAKEOUT_SE(agent, Hash40::new("se_yoshi_special_h01"));
    }
}