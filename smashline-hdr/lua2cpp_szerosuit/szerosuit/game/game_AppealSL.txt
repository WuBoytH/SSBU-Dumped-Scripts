#[acmd_script( agent = "szerosuit", script = "game_appealsl", category = ACMD_GAME, low_priority )]
unsafe fn game_appealsl(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        ArticleModule::generate_article(boma, *FIGHTER_SZEROSUIT_GENERATE_ARTICLE_WHIP, false, -1);
        methodlib::L2CValue::as_hash()const(*FIGHTER_SZEROSUIT_GENERATE_ARTICLE_WHIP, Hash40::new("appeal_s"));
        ArticleModule::change_motion(boma);
    }
    frame(lua_state, 81.0);
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_SZEROSUIT_GENERATE_ARTICLE_WHIP);
    }
}