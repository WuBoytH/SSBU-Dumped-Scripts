#[acmd_script( agent = "szerosuit_paralyzer_bullet", script = "sound_shoottame", category = ACMD_SOUND, low_priority )]
unsafe fn sound_shoottame(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_TAKEOUT_SE(agent, Hash40::new("se_szerosuit_special_n02"));
    }
}