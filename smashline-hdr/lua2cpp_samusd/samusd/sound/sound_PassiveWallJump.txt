#[acmd_script( agent = "samusd", script = "sound_passivewalljump", category = ACMD_SOUND, low_priority )]
unsafe fn sound_passivewalljump(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_samusd_jump03"));
    }
}