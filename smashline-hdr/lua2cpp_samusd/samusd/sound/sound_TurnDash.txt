#[acmd_script( agent = "samusd", script = "sound_turndash", category = ACMD_SOUND, low_priority )]
unsafe fn sound_turndash(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_samusd_dash_turn"));
        SET_PLAY_INHIVIT(agent, Hash40::new("se_samusd_dash_turn"), 20);
    }
}