#[acmd_script( agent = "samusd", script = "sound_slipescapeb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_slipescapeb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_slip_02"));
        PLAY_STATUS(agent, Hash40::new("se_samusd_sliprise"));
    }
    frame(lua_state, 17.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_samusd_sliprise"));
    }
    frame(lua_state, 21.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_samusd_landing01"));
    }
}