#[acmd_script( agent = "pichu", script = "sound_itemheavywalk", category = ACMD_SOUND, low_priority )]
unsafe fn sound_itemheavywalk(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_pichu_step_left_m"));
    }
    frame(lua_state, 15.0);
    PLAY_STEP(agent, 0x10bed9c2d4);
}