#[acmd_script( agent = "pichu", script = "sound_downbacku", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downbacku(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pichu_jump02"));
    }
    frame(lua_state, 19.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_pichu_landing01"));
    }
}