#[acmd_script( agent = "pichu_vortex", script = "effect_deactivate", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_deactivate(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("pichu_final_sphere"), false, false);
    }
}