#[acmd_script( agent = "demon", script = "sound_attack15", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attack15(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 10.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_demon_swing_short02"));
    }
}