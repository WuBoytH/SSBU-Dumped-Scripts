#[acmd_script( agent = "demon", script = "sound_batswing4", category = ACMD_SOUND, low_priority )]
unsafe fn sound_batswing4(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 45.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_demon_rnd_airattack_lw"));
    }
}