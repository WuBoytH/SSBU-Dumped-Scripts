#[acmd_script( agent = "demon", script = "sound_attackragedrive", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackragedrive(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_demon_special_l03"));
    }
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_demon_ragedrive_swing"));
    }
}