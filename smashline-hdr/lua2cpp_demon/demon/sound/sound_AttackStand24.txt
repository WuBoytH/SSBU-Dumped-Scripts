#[acmd_script( agent = "demon", script = "sound_attackstand24", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackstand24(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 13.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_demon_attackstand21"));
        PLAY_SEQUENCE(agent, Hash40::new("seq_demon_rnd_attack_s_03"));
    }
}