#[acmd_script( agent = "demon", script = "sound_downdamaged", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downdamaged(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_DOWN_SE(agent, Hash40::new("se_demon_down"));
    }
}