unsafe extern "C" fn effect_attackairf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        EFFECT_FOLLOW_NO_STOP(agent, Hash40::new("demon_attack_speedline_kick"), Hash40::new("top"), -1, 9.5, 1, 12, 4, 0, 0.65, true);
        LAST_EFFECT_SET_ALPHA(agent, 0.8);
        LAST_EFFECT_SET_RATE(agent, 1.4);
    }
}