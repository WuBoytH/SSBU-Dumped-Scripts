#[acmd_script( agent = "packun", script = "game_run", category = ACMD_GAME, low_priority )]
unsafe fn game_run(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area(boma, 10.0, 5.0);
    }
}