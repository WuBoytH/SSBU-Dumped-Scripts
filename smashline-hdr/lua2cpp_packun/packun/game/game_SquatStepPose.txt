#[acmd_script( agent = "packun", script = "game_squatsteppose", category = ACMD_GAME, low_priority )]
unsafe fn game_squatsteppose(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    WorkModule::is_flag(boma, *FIGHTER_PACKUN_STATUS_SQUAT_TREAD_DAMAGE_FLAG_JUMP_MINI);
    if(methodlib::L2CValue::operator==(lib::L2CValueconst&)const(false, false)){
        FT_MOTION_RATE(agent, 0.7);
    }
}