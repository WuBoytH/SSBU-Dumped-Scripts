#[acmd_script( agent = "packun", script = "game_turnrunbrake", category = ACMD_GAME, low_priority )]
unsafe fn game_turnrunbrake(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area(boma, 9.0, 5.0);
    }
    frame(lua_state, 9.0);
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area(boma, 6.0, 4.0);
    }
}