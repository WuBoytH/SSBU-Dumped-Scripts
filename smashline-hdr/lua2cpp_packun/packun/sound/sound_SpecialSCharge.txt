#[acmd_script( agent = "packun", script = "sound_specialscharge", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialscharge(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_packun_special_s01"));
    }
}