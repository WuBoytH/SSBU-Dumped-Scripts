#[acmd_script( agent = "packun", script = "sound_landingairn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landingairn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_packun_attackair_n01"));
        PLAY_LANDING_SE(agent, Hash40::new("se_packun_landing01"));
    }
}