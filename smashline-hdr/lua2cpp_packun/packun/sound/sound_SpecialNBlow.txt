#[acmd_script( agent = "packun", script = "sound_specialnblow", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialnblow(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_packun_special_n02"));
    }
}