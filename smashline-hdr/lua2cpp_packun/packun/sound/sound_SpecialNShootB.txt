#[acmd_script( agent = "packun", script = "sound_specialnshootb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialnshootb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_packun_special_n03"));
    }
}