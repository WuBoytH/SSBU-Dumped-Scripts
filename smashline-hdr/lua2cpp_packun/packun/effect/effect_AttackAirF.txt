#[acmd_script( agent = "packun", script = "effect_attackairf", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_attackairf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 10.0);
    if sv_animcmd::get_value_float(agent, *SO_VAR_FLOAT_LR) < 0.0 {
        if is_excute(agent) {
            EFFECT_FOLLOW(agent, Hash40::new("packun_atk_air_f_arc"), Hash40::new("top"), -2, 11, 6.5, -180, 200, -60, 1.3, true);
        }
        else{
        if is_excute(agent) {
            EFFECT_FOLLOW(agent, Hash40::new("packun_atk_air_f_arc"), Hash40::new("top"), 2, 11, 6.5, 0, -20, 60, 1.3, true);
        }
    }
}
frame(lua_state, 16.0);
if is_excute(agent) {
    EFFECT_OFF_KIND(agent, Hash40::new("packun_atk_air_f_arc"), true, true);
}
}