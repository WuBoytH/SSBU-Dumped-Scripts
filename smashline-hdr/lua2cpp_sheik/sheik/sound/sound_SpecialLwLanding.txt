#[acmd_script( agent = "sheik", script = "sound_speciallwlanding", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallwlanding(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_sheik_landing01"));
    }
    wait(lua_state, 18.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_sheik_landing02"));
    }
}