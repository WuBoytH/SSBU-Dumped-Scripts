#[acmd_script( agent = "sheik", script = "sound_attack100", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attack100(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_sheik_attack100"));
    }
}