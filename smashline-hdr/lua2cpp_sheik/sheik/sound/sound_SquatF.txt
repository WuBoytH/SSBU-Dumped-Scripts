#[acmd_script( agent = "sheik", script = "sound_squatf", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squatf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 10.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_sheik_crawing_left"));
    }
    frame(lua_state, 19.0);
    PLAY_STEP(agent, Hash40::new("se_sheik_crawing_right"));
}