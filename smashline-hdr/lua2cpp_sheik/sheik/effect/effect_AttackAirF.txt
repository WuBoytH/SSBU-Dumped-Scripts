#[acmd_script( agent = "sheik", script = "effect_attackairf", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_attackairf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("sheik_attack_cut"), Hash40::new("top"), -1.5, 6.3, 7.7, 180, 200, 67, 0.75, false);
        LAST_EFFECT_SET_RATE(agent, 1.3);
    }
}