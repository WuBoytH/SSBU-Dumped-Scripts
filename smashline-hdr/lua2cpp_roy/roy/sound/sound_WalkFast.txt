#[acmd_script( agent = "roy", script = "sound_walkfast", category = ACMD_SOUND, low_priority )]
unsafe fn sound_walkfast(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 12.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_roy_step_left_m"));
    }
    frame(lua_state, 28.0);
    PLAY_STEP(agent, Hash40::new("se_roy_step_right_m"));
}