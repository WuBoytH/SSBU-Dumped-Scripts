unsafe extern "C" fn sound_run(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_roy_step_right_l"));
    }
    frame(lua_state, 10.0);
    PLAY_STEP(agent, 0x126f3d9fda);
}