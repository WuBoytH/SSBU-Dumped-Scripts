#[acmd_script( agent = "roy", script = "sound_landingairlw" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_landingairlw(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 4.0);
    if is_excute(fighter) {
        PLAY_LANDING_SE(fighter, Hash40::new("se_roy_landing02"));
    }
}