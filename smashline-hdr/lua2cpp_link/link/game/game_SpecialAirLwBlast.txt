#[acmd_script( agent = "link", script = "game_specialairlwblast", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairlwblast(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_LINK_STATUS_WORK_ID_FLAG_BOMB_BLAST);
    }
}