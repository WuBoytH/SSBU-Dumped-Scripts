#[acmd_script( agent = "link", script = "game_attackairlw2bound", category = ACMD_GAME, low_priority )]
unsafe fn game_attackairlw2bound(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        KineticModule::add_speed(boma, &Vector3f{x: 0.0, y: 1.33, z: 0.0});
    }
}