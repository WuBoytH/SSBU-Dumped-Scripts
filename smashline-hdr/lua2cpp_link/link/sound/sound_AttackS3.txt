#[acmd_script( agent = "link", script = "sound_attacks3", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacks3(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 15.0);
    if is_excute(fighter) {
        PLAY_SEQUENCE(fighter, Hash40::new("seq_link_rnd_attack"));
        PLAY_SE(fighter, Hash40::new("se_link_swing_l"));
    }
    frame(lua_state, 19.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_link_attackhard_s01"));
    }
}