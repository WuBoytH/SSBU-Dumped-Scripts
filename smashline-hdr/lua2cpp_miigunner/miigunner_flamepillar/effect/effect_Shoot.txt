#[acmd_script( agent = "miigunner_flamepillar", script = "effect_shoot", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_shoot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("miigunner_flame_bullet"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}