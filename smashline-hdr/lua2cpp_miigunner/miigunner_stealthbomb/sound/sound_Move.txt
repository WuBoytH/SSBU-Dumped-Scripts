#[acmd_script( agent = "miigunner_stealthbomb", script = "sound_move" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_move(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        PLAY_STATUS(fighter, Hash40::new("se_miigunner_special_c2_s01"));
    }
}