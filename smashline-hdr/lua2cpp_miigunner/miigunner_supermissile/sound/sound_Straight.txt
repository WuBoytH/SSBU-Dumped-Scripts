#[acmd_script( agent = "miigunner_supermissile", script = "sound_straight", category = ACMD_SOUND, low_priority )]
unsafe fn sound_straight(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_miigunner_special_c3_s04"));
    }
}