#[acmd_script( agent = "miigunner", script = "game_specialairn3end", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairn3end(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        ArticleModule::generate_article_enable(boma, *FIGHTER_MIIGUNNER_GENERATE_ARTICLE_GRENADELAUNCHER, false, -1);
    }
}