#[acmd_script( agent = "miigunner", script = "game_specials2end", category = ACMD_GAME, low_priority )]
unsafe fn game_specials2end(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_MIIGUNNER_STATUS_STEALTH_BOMB_FLAG_2);
    }
}