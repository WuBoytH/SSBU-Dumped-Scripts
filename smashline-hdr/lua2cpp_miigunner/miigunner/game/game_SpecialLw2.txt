#[acmd_script( agent = "miigunner", script = "game_speciallw2", category = ACMD_GAME, low_priority )]
unsafe fn game_speciallw2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    FT_MOTION_RATE(agent, 1.15);
    frame(lua_state, 8.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_MIIGUNNER_STATUS_GROUND_BOMB_FLAG_EXIST_BOMB_BURST);
    }
    frame(lua_state, 12.0);
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_MIIGUNNER_GENERATE_ARTICLE_GROUNDBOMB, false, -1);
    }
}