#[acmd_script( agent = "miigunner", script = "sound_speciallw3hold", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallw3hold(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_miigunner_special_c3_l01"));
    }
}