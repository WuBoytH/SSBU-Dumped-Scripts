#[acmd_script( agent = "miigunner", script = "sound_run", category = ACMD_SOUND, low_priority )]
unsafe fn sound_run(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_miigunner_step_right_m"));
    }
    frame(lua_state, 20.0);
    PLAY_STEP(agent, Hash40::new("se_miigunner_step_left_m"));
}