#[acmd_script( agent = "pikachu", script = "sound_walkfast", category = ACMD_SOUND, low_priority )]
unsafe fn sound_walkfast(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_pikachu_step_left_l"));
    }
    frame(lua_state, 11.0);
    PLAY_STEP(agent, Hash40::new("se_pikachu_step_right_l"));
}