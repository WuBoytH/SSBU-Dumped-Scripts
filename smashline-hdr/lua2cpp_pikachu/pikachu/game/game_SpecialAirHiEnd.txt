#[acmd_script( agent = "pikachu", script = "game_specialairhiend" , category = ACMD_GAME , low_priority)]
unsafe fn game_specialairhiend(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        notify_event_msc_cmd(0x2127e37c07, *GROUND_CLIFF_CHECK_KIND_ALWAYS_BOTH_SIDES);
        JostleModule::set_status(boma, true);
    }
    frame(lua_state, 9.0);
    if is_excute(fighter) {
        WorkModule::inc_int(boma, *FIGHTER_PIKACHU_STATUS_WORK_ID_INT_QUICK_ATTACK_PHASE);
    }
    frame(lua_state, 10.0);
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_PIKACHU_GENERATE_ARTICLE_SPECIALUPDUMMY);
    }
}