#[acmd_script( agent = "koopag", script = "game_cliffjump1", category = ACMD_GAME, low_priority )]
unsafe fn game_cliffjump1(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    FT_MOTION_RATE(agent, 8.0);
    frame(lua_state, 2.0);
    FT_MOTION_RATE(agent, 4.0);
}