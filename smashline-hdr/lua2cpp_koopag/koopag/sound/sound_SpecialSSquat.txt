unsafe extern "C" fn sound_specialssquat(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_koopag_special_s01"));
        PLAY_SE(agent, Hash40::new("se_koopag_special_s02"));
    }
    wait(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, 0x0dae3a633b);
    }
}