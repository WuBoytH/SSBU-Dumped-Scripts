#[acmd_script( agent = "koopag", script = "sound_specialairnstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairnstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_koopag_special_n01"));
    }
}