#[acmd_script( agent = "diddy", script = "sound_specialairnshoot", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairnshoot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_diddy_special_n02"));
    }
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_diddy_special_n01"));
    }
}