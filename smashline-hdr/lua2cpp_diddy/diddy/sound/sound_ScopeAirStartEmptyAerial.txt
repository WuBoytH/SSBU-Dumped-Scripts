#[acmd_script( agent = "diddy", script = "sound_scopeairstartemptyaerial", category = ACMD_SOUND, low_priority )]
unsafe fn sound_scopeairstartemptyaerial(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_diddy_jump02"));
    }
    wait(lua_state, 15.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_diddy_jumpround"));
    }
}