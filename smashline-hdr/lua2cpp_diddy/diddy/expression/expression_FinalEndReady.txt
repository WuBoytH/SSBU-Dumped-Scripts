#[acmd_script( agent = "diddy", script = "expression_finalendready", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_finalendready(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_31_cypher"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}