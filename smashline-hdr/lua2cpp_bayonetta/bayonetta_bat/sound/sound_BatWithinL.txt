#[acmd_script( agent = "bayonetta_bat", script = "sound_batwithinl", category = ACMD_SOUND, low_priority )]
unsafe fn sound_batwithinl(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_bayonetta_batwithin02"));
    }
}