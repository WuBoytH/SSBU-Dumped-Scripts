#[acmd_script( agent = "bayonetta", script = "sound_attacks32", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacks32(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 12.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_bayonetta_rnd_attack01"));
        PLAY_SE(agent, Hash40::new("se_bayonetta_attackhard_s02"));
    }
    frame(lua_state, 41.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_bayonetta_step_left_s"));
    }
}