#[acmd_script( agent = "bayonetta", script = "game_shootingarml_atkoff_specialhi", category = ACMD_GAME, low_priority )]
unsafe fn game_shootingarml_atkoff_specialhi(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        AttackModule::clear(boma, FIGHTER_BAYONETTA_SHOOTING_ATTACK_ID_01);
    }
}