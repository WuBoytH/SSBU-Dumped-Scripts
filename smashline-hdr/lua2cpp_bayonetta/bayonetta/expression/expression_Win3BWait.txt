#[acmd_script( agent = "bayonetta", script = "expression_win3bwait", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_win3bwait(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        VisibilityModule::set_int64(boma, hash40("gun_hand") as i64, hash40("gun_hand_show_all") as i64);
    }
}