#[acmd_script( agent = "bayonetta", script = "expression_win2await", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_win2await(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        VisibilityModule::set_int64(boma, Hash40::new("gun_hand"), Hash40::new("gun_hand_hide_all"));
    }
}