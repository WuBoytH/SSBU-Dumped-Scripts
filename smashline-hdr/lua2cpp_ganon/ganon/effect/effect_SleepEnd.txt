#[acmd_script( agent = "ganon", script = "effect_sleepend", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_sleepend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        EFFECT(agent, Hash40::new("sys_piyo"), Hash40::new("top"), 0, 13, 2, 0, 0, 0, 1.8, 0, 0, 0, 0, 0, 0, true);
    }
}