#[acmd_script( agent = "ganon", script = "sound_specialhicatch", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialhicatch(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_ganon_special_h02"));
        PLAY_SE(agent, Hash40::new("se_ganon_special_h03"));
    }
}