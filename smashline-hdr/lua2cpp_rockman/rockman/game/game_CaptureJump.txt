unsafe extern "C" fn game_capturejump(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        KineticModule::add_speed(boma, &Vector3f{x: -0.8, y: 0.0, z: 0.0});
    }
}