#[acmd_script( agent = "rockman", script = "game_specialairnempty", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairnempty(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 0.0);
}