#[acmd_script( agent = "rockman_blackhole", script = "effect_bomb", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_bomb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("rockman_final_bb_space"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}