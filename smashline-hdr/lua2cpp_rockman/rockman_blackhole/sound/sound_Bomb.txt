#[acmd_script( agent = "rockman_blackhole", script = "sound_bomb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_bomb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_rockman_final02"));
    }
}