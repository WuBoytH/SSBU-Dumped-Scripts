unsafe extern "C" fn game_exit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 8.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *WEAPON_ROCKMAN_RUSHCOIL_INSTANCE_WORK_ID_FLAG_EXIT);
    }
}