#[acmd_script( agent = "master", script = "expression_specialairnjumpcancel", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairnjumpcancel(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ItemModule::set_have_item_visibility(boma, false, 0);
        WorkModule::on_flag(boma, *FIGHTER_MASTER_INSTANCE_WORK_ID_FLAG_SWORD_OFF_EFFECT_AURA);
    }
}