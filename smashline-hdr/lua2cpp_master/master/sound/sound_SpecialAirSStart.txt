#[acmd_script( agent = "master", script = "sound_specialairsstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairsstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_master_special_s01"));
    }
}