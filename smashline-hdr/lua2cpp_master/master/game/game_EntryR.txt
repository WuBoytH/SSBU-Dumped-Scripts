#[acmd_script( agent = "master", script = "game_entryr", category = ACMD_GAME, low_priority )]
unsafe fn game_entryr(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        ArticleModule::generate_article(boma, *FIGHTER_MASTER_GENERATE_ARTICLE_BATON, false, -1);
        ArticleModule::change_motion(boma, *FIGHTER_MASTER_GENERATE_ARTICLE_BATON, Hash40::new("entry_r"), false, -1.0);
    }
    frame(lua_state, 87.0);
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_MASTER_GENERATE_ARTICLE_BATON);
    }
}