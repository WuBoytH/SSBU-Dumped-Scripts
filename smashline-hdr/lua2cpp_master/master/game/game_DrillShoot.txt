#[acmd_script( agent = "master", script = "game_drillshoot", category = ACMD_GAME, low_priority )]
unsafe fn game_drillshoot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 9.0);
    if is_excute(agent) {
        SHOOT_ITEM_BULLET(agent);
    }
}