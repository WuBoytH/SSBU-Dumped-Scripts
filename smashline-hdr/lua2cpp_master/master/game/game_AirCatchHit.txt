unsafe extern "C" fn game_aircatchhit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area_xy(boma, 1.0, 10.0, 1.0, 10.0);
    }
}