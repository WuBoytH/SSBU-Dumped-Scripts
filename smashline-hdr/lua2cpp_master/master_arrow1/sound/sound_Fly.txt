#[acmd_script( agent = "master_arrow1", script = "sound_fly", category = ACMD_SOUND, low_priority )]
unsafe fn sound_fly(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_master_special_n05"));
    }
}