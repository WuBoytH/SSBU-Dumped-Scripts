#[acmd_script( agent = "gaogaen_rope", script = "sound_bend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_bend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 12.0);
}