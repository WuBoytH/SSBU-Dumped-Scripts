#[acmd_script( agent = "gaogaen", script = "expression_attacks4shit", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_attacks4shit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 20.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_elecattack"), 36, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}