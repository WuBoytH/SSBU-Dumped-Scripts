#[acmd_script( agent = "gaogaen", script = "sound_attacks4shit", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacks4shit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 9.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_gaogaen_landing01"));
    }
    frame(lua_state, 16.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_gaogaen_smash_s02"));
    }
    frame(lua_state, 18.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_gaogaen_rnd_attackappeal01"));
    }
    frame(lua_state, 23.0);
    if is_excute(agent) {
        PLAY_SE_NO_3D(agent, Hash40::new("se_gaogaen_react_success"));
    }
}