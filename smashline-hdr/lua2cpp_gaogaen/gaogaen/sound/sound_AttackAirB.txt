#[acmd_script( agent = "gaogaen", script = "sound_attackairb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackairb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_gaogaen_rnd_attack"));
        PLAY_SE(agent, Hash40::new("se_gaogaen_attackair_b01"));
    }
}