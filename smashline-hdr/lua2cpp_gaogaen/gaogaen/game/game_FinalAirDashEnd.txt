#[acmd_script( agent = "gaogaen", script = "game_finalairdashend", category = ACMD_GAME, low_priority )]
unsafe fn game_finalairdashend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WHOLE_HIT(agent, *HIT_STATUS_XLU);
        SET_SPEED_EX(agent, 3, 0, *KINETIC_ENERGY_RESERVE_ATTRIBUTE_MAIN);
    }
    frame(lua_state, 15.0);
    FT_MOTION_RATE(agent, 0.4);
    frame(lua_state, 55.0);
    FT_MOTION_RATE(agent, 1.0);
}