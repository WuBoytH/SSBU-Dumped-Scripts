#[acmd_script( agent = "edge_flare2", script = "sound_try", category = ACMD_SOUND, low_priority )]
unsafe fn sound_try(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_edge_special_s04"));
        PLAY_SE(agent, Hash40::new("se_edge_special_s05"));
    }
}