#[acmd_script( agent = "edge", script = "effect_wingstart", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_wingstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EffectModule::req_screen(boma, Hash40::new("edge_wing_screen"), false, false, false);
        EFFECT_FOLLOW(agent, Hash40::new("edge_onewinged_start"), Hash40::new("wingr01"), 1.5, 0, -1, 0, 0, 0, 1, false);
    }
}