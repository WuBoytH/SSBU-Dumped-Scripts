#[acmd_script( agent = "edge", script = "sound_landinglight", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landinglight(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 2.0);
    if WorkModule::is_flag(boma, *FIGHTER_EDGE_INSTANCE_WORK_ID_FLAG_ONE_WINGED_ACTIVATED) {
        if is_excute(fighter) {
            PLAY_LANDING_SE(fighter, Hash40::new("se_edge_landing01"));
            PLAY_SE(fighter, Hash40::new("se_edge_winged_landing01"));
        }
        else{
        if is_excute(fighter) {
            PLAY_LANDING_SE(fighter, Hash40::new("se_edge_landing01"));
        }
    }
}
}