#[acmd_script( agent = "edge", script = "sound_win1a", category = ACMD_SOUND, low_priority )]
unsafe fn sound_win1a(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 18.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_edge_win01"));
    }
}