#[acmd_script( agent = "edge", script = "sound_walkfast", category = ACMD_SOUND, low_priority )]
unsafe fn sound_walkfast(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 11.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_edge_step_right_l"));
    }
    frame(lua_state, 32.0);
    PLAY_STEP(agent, 0x135c609f41);
}