#[acmd_script( agent = "edge", script = "sound_specialhi2end", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialhi2end(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_edge_special_h03_08"));
    }
    frame(lua_state, 2.0);
    if WorkModule::is_flag(boma, *FIGHTER_EDGE_INSTANCE_WORK_ID_FLAG_ONE_WINGED_ACTIVATED) {
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_edge_winged_special_h01"));
        }
    }
}