#[acmd_script( agent = "edge", script = "expression_attackairfwall" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_attackairfwall(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_attackm"), 0, false);
		ControlModule::set_rumble(boma);
		QUAKE(fighter, *CAMERA_QUAKE_KIND_S);
	}
}