#[acmd_script( agent = "ridley", script = "effect_scopeairrapidfly2", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_scopeairrapidfly2(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if WorkModule::is_flag(boma, *FIGHTER_STATUS_ITEM_SHOOT_WORK_FLAG_SSCOPE_AERIAL_REQ_EFFECT) {
        if is_excute(fighter) {
            WorkModule::off_flag(boma, *FIGHTER_STATUS_ITEM_SHOOT_WORK_FLAG_SSCOPE_AERIAL_REQ_EFFECT);
        }
    }
    methodlua2cpp::L2CFighterAnimcmdEffectCommon::effect_ScopeAirRapidCommon()();
}