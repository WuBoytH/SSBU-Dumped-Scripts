#[acmd_script( agent = "ridley", script = "game_specialairscatch", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairscatch(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        KineticModule::add_speed(boma, &Vector3f{x: 0.5, y: 1.5, z: 0.0});
        FT_CATCH_STOP(agent, 6, 1);
    }
}