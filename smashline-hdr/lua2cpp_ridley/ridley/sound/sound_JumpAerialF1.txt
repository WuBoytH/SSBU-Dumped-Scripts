#[acmd_script( agent = "ridley", script = "sound_jumpaerialf1", category = ACMD_SOUND, low_priority )]
unsafe fn sound_jumpaerialf1(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_ridley_jump01"));
    }
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ridley_jump02"));
    }
}