#[acmd_script( agent = "ridley", script = "sound_sleepstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleepstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_common_sleep"));
    }
}