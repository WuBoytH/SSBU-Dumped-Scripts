#[acmd_script( agent = "ridley", script = "expression_specialairnhold", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairnhold(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
        ControlModule::set_rumble(boma, Hash40::new("rbkind_65_breath"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}