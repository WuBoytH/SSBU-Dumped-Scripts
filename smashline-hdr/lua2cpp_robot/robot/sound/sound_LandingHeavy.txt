#[acmd_script( agent = "robot", script = "sound_landingheavy", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landingheavy(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_robot_special_h01"));
        PLAY_LANDING_SE(agent, Hash40::new("se_robot_landing02"));
    }
}