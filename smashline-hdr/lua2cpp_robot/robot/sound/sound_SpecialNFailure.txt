#[acmd_script( agent = "robot", script = "sound_specialnfailure", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialnfailure(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE_REMAIN(agent, Hash40::new("se_robot_special_n03"));
    }
}