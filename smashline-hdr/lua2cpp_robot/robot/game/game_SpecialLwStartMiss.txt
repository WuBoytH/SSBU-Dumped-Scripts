#[acmd_script( agent = "robot", script = "game_speciallwstartmiss", category = ACMD_GAME, low_priority )]
unsafe fn game_speciallwstartmiss(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_ROBOT_GENERATE_ARTICLE_GYRO_HOLDER, false, -1);
    }
}