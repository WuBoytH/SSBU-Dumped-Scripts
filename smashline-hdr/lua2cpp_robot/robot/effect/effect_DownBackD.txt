unsafe extern "C" fn effect_downbackd(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    for _ in 0..4 {
    if is_excute(agent) {
        EFFECT(agent, Hash40::new("sys_slip_spark"), Hash40::new("top"), 2, 0, 0, 0, 180, 0, 0.8, 0, 0, 0, 0, 0, 0, false);
    }
    wait(lua_state, 5.0);
}
}