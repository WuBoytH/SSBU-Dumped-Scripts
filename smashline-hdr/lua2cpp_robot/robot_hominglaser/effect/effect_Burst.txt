#[acmd_script( agent = "robot_hominglaser", script = "effect_burst", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_burst(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("sys_bomb_a"), Hash40::new("top"), -0.0, 0, 0, 0, 0, 0, 0.7, true);
    }
}