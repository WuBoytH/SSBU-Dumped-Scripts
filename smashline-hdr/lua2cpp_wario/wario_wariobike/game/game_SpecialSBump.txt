#[acmd_script( agent = "wario_wariobike", script = "game_specialsbump", category = ACMD_GAME, low_priority )]
unsafe fn game_specialsbump(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        AttackModule::clear_all(boma);
        WorkModule::on_flag(boma, *WEAPON_WARIO_WARIOBIKE_STATUS_WORK_FLAG_BUMP_WARIO_AWAY);
    }
}