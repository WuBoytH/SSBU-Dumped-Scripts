#[acmd_script( agent = "wario", script = "expression_specialairnopenwait", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairnopenwait(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_TOP, 4, true);
    }
}