#[acmd_script( agent = "wario", script = "sound_specialairneat", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairneat(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        PLAY_SE_REMAIN(agent, Hash40::new("se_wario_special_n03"));
    }
}