#[acmd_script( agent = "wario", script = "sound_squatb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squatb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 13.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_wario_crawing_left_b"));
    }
    wait(lua_state, 20.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_wario_crawing_right_b"));
    }
    wait(lua_state, 1.0);
}