#[acmd_script( agent = "mario_fireball", script = "sound_regular", category = ACMD_SOUND, low_priority )]
unsafe fn sound_regular(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        REG_LANDING_SE(agent, Hash40::new("se_mario_special_n02"));
        SET_TAKEOUT_SE(agent, Hash40::new("se_mario_special_n01"));
    }
}