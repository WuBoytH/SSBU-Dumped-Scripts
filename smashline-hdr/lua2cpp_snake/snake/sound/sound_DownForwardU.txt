unsafe extern "C" fn sound_downforwardu(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_snake_dash_start"));
    }
    frame(lua_state, 14.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_snake_escape"));
    }
    frame(lua_state, 24.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_snake_landing01"));
    }
}