#[acmd_script( agent = "snake", script = "sound_squat", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squat(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_snake_squat"));
        PLAY_SE(agent, Hash40::new("se_snake_squat_gear"));
        SET_PLAY_INHIVIT(agent, Hash40::new("se_snake_squat"), 15);
    }
}