#[acmd_script( agent = "snake", script = "expression_attack11_cancel", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_attack11_cancel(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_snake_swing_s"));
    }
}