#[acmd_script( agent = "snake_c4", script = "sound_stickother", category = ACMD_SOUND, low_priority )]
unsafe fn sound_stickother(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if WorkModule::is_flag(boma, *WEAPON_SNAKE_C4_INSTANCE_WORK_ID_FLAG_C3) {
        if is_excute(fighter) {
            PLAY_SE(fighter, Hash40::new("se_snake_special_l07"));
        }
        else{
        if is_excute(fighter) {
            PLAY_SE(fighter, Hash40::new("se_snake_special_l02"));
        }
    }
}
}