#[acmd_script( agent = "snake_nikita", script = "game_shoot", category = ACMD_GAME, low_priority )]
unsafe fn game_shoot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *WEAPON_SNAKE_NIKITA_GENERATE_ARTICLE_MISSILE, false, -1);
    }
}