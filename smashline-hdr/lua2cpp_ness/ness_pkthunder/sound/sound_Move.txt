#[acmd_script( agent = "ness_pkthunder", script = "sound_move", category = ACMD_SOUND, low_priority )]
unsafe fn sound_move(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_TAKEOUT_SE_STATUS(agent, Hash40::new("se_ness_special_h02"));
        PLAY_STATUS(agent, Hash40::new("se_ness_special_h02"));
    }
}