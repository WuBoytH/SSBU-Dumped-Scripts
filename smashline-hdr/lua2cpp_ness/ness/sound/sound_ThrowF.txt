#[acmd_script( agent = "ness", script = "sound_throwf", category = ACMD_SOUND, low_priority )]
unsafe fn sound_throwf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ness_throw_pk01"));
        PLAY_SE(agent, Hash40::new("se_common_throw_01"));
    }
    wait(lua_state, 15.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ness_throw_pk02"));
    }
    wait(lua_state, 12.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_ness_rnd_attack"));
        PLAY_SE(agent, Hash40::new("se_common_throw_02"));
    }
}