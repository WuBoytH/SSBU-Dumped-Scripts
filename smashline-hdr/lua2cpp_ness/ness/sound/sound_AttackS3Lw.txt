#[acmd_script( agent = "ness", script = "sound_attacks3lw", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacks3lw(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_ness_rnd_attack"));
        PLAY_SE(agent, Hash40::new("se_ness_attackhard_s01"));
    }
}