unsafe extern "C" fn sound_shoot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_TAKEOUT_SE(agent, Hash40::new("se_ness_special_s01"));
        PLAY_SE_REMAIN(agent, Hash40::new("se_ness_special_s01"));
    }
}