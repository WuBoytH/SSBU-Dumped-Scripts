#[acmd_script( agent = "ness_pkfire", script = "sound_pillarair", category = ACMD_SOUND, low_priority )]
unsafe fn sound_pillarair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_ness_special_s02"));
    }
}