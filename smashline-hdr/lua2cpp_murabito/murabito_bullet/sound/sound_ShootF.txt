#[acmd_script( agent = "murabito_bullet", script = "sound_shootf", category = ACMD_SOUND, low_priority )]
unsafe fn sound_shootf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_murabito_attackair_f02"));
    }
}