#[acmd_script( agent = "murabito_tomnook", script = "effect_constructendr", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_constructendr(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 22.0);
    if is_excute(agent) {
        WorkModule::off_flag(boma, *WEAPON_MURABITO_TOMNOOK_INSTANCE_WORK_ID_FLAG_VIS_WHOLE);
    }
}