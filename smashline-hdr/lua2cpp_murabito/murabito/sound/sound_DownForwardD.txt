#[acmd_script( agent = "murabito", script = "sound_downforwardd", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downforwardd(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_murabito_escape"));
    }
    frame(lua_state, 22.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_murabito_landing02"));
    }
}