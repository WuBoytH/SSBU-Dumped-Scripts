#[acmd_script( agent = "murabito", script = "sound_specialnhand", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialnhand(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_murabito_special_n02"));
    }
}