#[acmd_script( agent = "murabito", script = "game_finalairendr", category = ACMD_GAME, low_priority )]
unsafe fn game_finalairendr(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WHOLE_HIT(agent, *HIT_STATUS_XLU);
    }
    frame(lua_state, 1.0);
    FT_MOTION_RATE(agent, 0.8);
    frame(lua_state, 30.0);
    FT_MOTION_RATE(agent, 1.0);
    frame(lua_state, 40.0);
    FT_MOTION_RATE(agent, 2.2);
}