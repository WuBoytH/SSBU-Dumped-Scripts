unsafe extern "C" fn game_finalmoneyl(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 30.0);
    if is_excute(agent) {
        FILL_SCREEN_MODEL_COLOR(agent, 1, 10, 0.3, 0.3, 0.3, 0, 0, 0, 1, 1.3, EffectScreenLayer:*GROUND, *EFFECT_SCREEN_PRIO_FINAL);
    }
}