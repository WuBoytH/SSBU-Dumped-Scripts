#[acmd_script( agent = "murabito_sprinkling_water", script = "effect_flow", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_flow(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if WorkModule::is_flag(boma, *WEAPON_MURABITO_SPRINKLING_WATER_INSTANCE_WORK_ID_FLAG_EFFECT_REQUEST) {
        if is_excute(agent) {
            EFFECT_FOLLOW(agent, Hash40::new("murabito_water"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
        }
    }
}