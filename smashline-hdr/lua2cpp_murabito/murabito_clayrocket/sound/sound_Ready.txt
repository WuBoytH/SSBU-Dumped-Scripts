#[acmd_script( agent = "murabito_clayrocket", script = "sound_ready", category = ACMD_SOUND, low_priority )]
unsafe fn sound_ready(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_murabito_special_s05"));
    }
}