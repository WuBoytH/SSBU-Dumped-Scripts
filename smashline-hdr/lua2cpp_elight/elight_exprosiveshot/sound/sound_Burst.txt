#[acmd_script( agent = "elight_exprosiveshot", script = "sound_burst", category = ACMD_SOUND, low_priority )]
unsafe fn sound_burst(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_elight_special_h02_01"));
        PLAY_SE(agent, Hash40::new("se_elight_special_h02_02"));
    }
}