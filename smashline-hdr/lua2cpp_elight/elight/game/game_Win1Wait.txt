#[acmd_script( agent = "elight", script = "game_win1wait", category = ACMD_GAME, low_priority )]
unsafe fn game_win1wait(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if !WorkModule::is_flag(boma, *FIGHTER_INSTANCE_WORK_ID_FLAG_RESULT_FIGHTER_SINGLE) {
        if is_excute(fighter) {
            ArticleModule::change_motion(boma, *FIGHTER_ELIGHT_GENERATE_ARTICLE_DIVER, Hash40::new("l_win_1_wait"), false, -1.0);
        }
    }
}