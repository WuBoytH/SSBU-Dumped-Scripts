#[acmd_script( agent = "elight", script = "game_damagehi2", category = ACMD_GAME, low_priority )]
unsafe fn game_damagehi2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        VisibilityModule::set_whole(boma, true);
    }
}