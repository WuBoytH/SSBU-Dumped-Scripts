#[acmd_script( agent = "elight", script = "game_drillshootair", category = ACMD_GAME, low_priority )]
unsafe fn game_drillshootair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 9.0);
    if is_excute(agent) {
        SHOOT_ITEM_BULLET(agent);
    }
}