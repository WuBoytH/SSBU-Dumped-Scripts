#[acmd_script( agent = "pacman_firehydrantwater", script = "effect_shootup", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_shootup(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WorkModule::set_int(boma, 6, *WEAPON_PACMAN_FIREHYDRANT_WATER_INSTANCE_WORK_ID_INT_HIT_EFFECT_FRAME);
    }
}