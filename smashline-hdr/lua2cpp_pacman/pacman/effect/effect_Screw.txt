#[acmd_script( agent = "pacman", script = "effect_screw", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_screw(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("sys_screw"), Hash40::new("rot"), 0, -1, -1, 0, 0, 0, 1.2, true);
    }
}