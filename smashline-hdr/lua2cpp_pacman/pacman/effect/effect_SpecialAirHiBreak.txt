#[acmd_script( agent = "pacman", script = "effect_specialairhibreak", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairhibreak(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("pacman_change_start"), Hash40::new("rot"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}