#[acmd_script( agent = "sonic", script = "sound_specialsspin", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialsspin(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_sonic_cliffclimb"));
    }
}