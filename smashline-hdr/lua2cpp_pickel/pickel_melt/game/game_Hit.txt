#[acmd_script( agent = "pickel_melt", script = "game_hit" , category = ACMD_GAME , low_priority)]
unsafe fn game_hit(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_77_attackm"), 0, false);
		ControlModule::set_rumble(boma);
	}
}