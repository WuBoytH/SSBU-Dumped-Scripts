#[acmd_script( agent = "pickel", script = "sound_specialhilandingstand" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialhilandingstand(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		PLAY_STEP(fighter, Hash40::new("se_pickel_step_l"));
	}
}