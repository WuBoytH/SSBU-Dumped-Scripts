#[acmd_script( agent = "pickel", script = "effect_squatf", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_squatf(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    for _ in 0..4 {
    if is_excute(agent) {
        FOOT_EFFECT(agent, Hash40::new("null"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 0.7, 0, 0, 0, 0, 0, 0, false);
    }
    wait(lua_state, 25.0);
}
}