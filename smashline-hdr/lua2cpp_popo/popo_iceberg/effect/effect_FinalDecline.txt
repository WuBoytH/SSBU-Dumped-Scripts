#[acmd_script( agent = "popo_iceberg", script = "effect_finaldecline", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finaldecline(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 50.0);
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("popo_final_cold_smoke"), false, true);
        CANCEL_FILL_SCREEN(agent, 0, 30);
    }
}