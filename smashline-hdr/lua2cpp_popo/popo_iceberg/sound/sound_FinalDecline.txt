#[acmd_script( agent = "popo_iceberg", script = "sound_finaldecline", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finaldecline(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_popo_final01"));
        PLAY_SE(agent, Hash40::new("se_popo_final04"));
    }
}