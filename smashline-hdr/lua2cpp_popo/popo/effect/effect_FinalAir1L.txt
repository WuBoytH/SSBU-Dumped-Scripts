#[acmd_script( agent = "popo", script = "effect_finalair1l" , category = ACMD_EFFECT , low_priority)]
unsafe fn effect_finalair1l(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("bg_popo_final"), false);
        EffectModule::req_screen(boma);
    }
}