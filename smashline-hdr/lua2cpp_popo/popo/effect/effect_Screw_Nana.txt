#[acmd_script( agent = "popo", script = "effect_screw_nana", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_screw_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("sys_screw"), Hash40::new("rot"), 0, 0, 0, 0, 0, 0, 1.2, true);
    }
}