#[acmd_script( agent = "popo", script = "effect_specialhithrownana_nana", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialhithrownana_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("popo_gomjump_throw"), Hash40::new("rot"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}