#[acmd_script( agent = "popo", script = "game_itemlightthrowdash_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_itemlightthrowdash_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    FT_MOTION_RATE(agent, 0.92);
    frame(lua_state, 4.0);
    if is_excute(agent) {
        THROW_ITEM_OFFSET(agent, 8, 10, *ITEM_FIGHTER_VAR_FLOAT_ITEM_THROW_ANGLE, *ITEM_FIGHTER_VAR_FLOAT_ITEM_THROW_SPEED, *ITEM_FIGHTER_VAR_FLOAT_ITEM_THROW_POWER);
    }
}