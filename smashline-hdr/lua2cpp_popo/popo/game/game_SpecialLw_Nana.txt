#[acmd_script( agent = "popo", script = "game_speciallw_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_speciallw_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 16.0);
    for _ in 0..9 {
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_POPO_GENERATE_ARTICLE_BLIZZARD, false, -1);
    }
    wait(lua_state, 5.0);
}
frame(lua_state, 70.0);
if is_excute(agent) {
    WorkModule::on_flag(boma, *FIGHTER_POPO_STATUS_SPECIAL_LW_FLAG_ENABLE_COUPLE);
}
}