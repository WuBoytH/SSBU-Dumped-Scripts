#[acmd_script( agent = "popo", script = "game_specialairhithrow_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairhithrow_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_POPO_STATUS_SPECIAL_HI_FLAG_FAIL_CLIFF_START);
    }
}