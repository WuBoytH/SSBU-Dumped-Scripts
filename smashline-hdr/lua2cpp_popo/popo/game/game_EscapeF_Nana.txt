#[acmd_script( agent = "popo", script = "game_escapef_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_escapef_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 20.0);
    if is_excute(agent) {
        REVERSE_LR(agent);
    }
}