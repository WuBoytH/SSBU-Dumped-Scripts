#[acmd_script( agent = "popo", script = "expression_itemlightthrowairf_nana", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_itemlightthrowairf_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_L);
    }
}