#[acmd_script( agent = "popo", script = "expression_swim_nana", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_swim_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 30.0);
}