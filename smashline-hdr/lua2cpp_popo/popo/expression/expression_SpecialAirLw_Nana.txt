#[acmd_script( agent = "popo", script = "expression_specialairlw_nana" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_specialairlw_nana(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_L, 4);
        ItemModule::set_have_item_visibility(boma, false);
    }
    frame(lua_state, 16.0);
    if is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_elecattack"), 40, true);
        ControlModule::set_rumble(boma);
    }
    frame(lua_state, 76.0);
    if is_excute(fighter) {
        ItemModule::set_have_item_visibility(boma, true);
    }
}