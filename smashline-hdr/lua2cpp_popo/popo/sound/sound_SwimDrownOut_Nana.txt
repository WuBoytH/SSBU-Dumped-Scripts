#[acmd_script( agent = "popo", script = "sound_swimdrownout_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_swimdrownout_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_swimdrown"));
    }
}