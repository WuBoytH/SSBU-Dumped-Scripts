#[acmd_script( agent = "popo", script = "sound_landingheavy_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landingheavy_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_popo_landing02"));
    }
}