#[acmd_script( agent = "popo", script = "sound_dragoonride_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_dragoonride_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if WorkModule::is_flag(boma, *FIGHTER_POPO_INSTANCE_WORK_ID_FLAG_MAIN_FIGHTER_NANA) {
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_common_dragoon_ride"));
        }
    }
}