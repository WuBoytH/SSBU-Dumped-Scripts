#[acmd_script( agent = "popo", script = "sound_sleeploop_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleeploop_nana(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("vc_nana_furasleep"));
    }
    wait(lua_state, 1.0);
}