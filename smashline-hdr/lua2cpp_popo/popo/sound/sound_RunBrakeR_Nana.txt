#[acmd_script( agent = "popo", script = "sound_runbraker_nana", category = ACMD_SOUND, low_priority)]
unsafe fn sound_runbraker_nana(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_popo_dash_stop"));
        SET_PLAY_INHIVIT(fighter, Hash40::new("se_popo_dash_stop"), 10);
    }
}