#[acmd_script( agent = "zelda", script = "game_itemlightwalkget", category = ACMD_GAME, low_priority )]
unsafe fn game_itemlightwalkget(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        ItemModule::pickup_item(boma, *ITEM_SIZE_LIGHT);
    }
}