#[acmd_script( agent = "zelda", script = "effect_finalairfinish", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalairfinish(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    frame(lua_state, 40.0);
    if is_excute(agent) {
        EffectModule::remove_screen(boma, Hash40::new("bg_zelda_final"), -1);
    }
}