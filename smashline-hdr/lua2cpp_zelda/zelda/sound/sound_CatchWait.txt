#[acmd_script( agent = "zelda", script = "sound_catchwait", category = ACMD_SOUND, low_priority )]
unsafe fn sound_catchwait(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_zelda_catchloop"));
    }
}