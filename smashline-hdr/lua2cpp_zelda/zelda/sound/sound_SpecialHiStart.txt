#[acmd_script( agent = "zelda", script = "sound_specialhistart" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialhistart(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    frame(lua_state, 2.0);
    if is_excute(fighter) {
        PLAY_STATUS(fighter, Hash40::new("se_zelda_special_h01"));
    }
}