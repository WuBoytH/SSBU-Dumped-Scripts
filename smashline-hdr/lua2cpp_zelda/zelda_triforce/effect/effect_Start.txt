unsafe extern "C" fn effect_start(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 38.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("zelda_final_impact"), Hash40::new("triforce"), 0, 0, 0, 0, 0, 0, 0.95, false);
    }
}