#[acmd_script( agent = "dedede", script = "expression_specialairhiturnr", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairhiturnr(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 11.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_escape_st"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}