#[acmd_script( agent = "dedede", script = "effect_specialairhiturnr", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairhiturnr(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("dedede_superjump_fall"), true, true);
    }
}