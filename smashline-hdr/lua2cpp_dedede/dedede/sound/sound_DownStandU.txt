#[acmd_script( agent = "dedede", script = "sound_downstandu", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downstandu(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_dedede_step_left_s"));
    }
    frame(lua_state, 18.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_dedede_step_right_m"));
    }
    frame(lua_state, 25.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_dedede_step_right_s"));
    }
}