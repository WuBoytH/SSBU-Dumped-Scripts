#[acmd_script( agent = "dedede", script = "sound_finalvisualscene02", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finalvisualscene02(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 28.0);
    if is_excute(agent) {
        PLAY_SE_NO_3D(agent, Hash40::new("vc_dedede_final02"));
    }
}