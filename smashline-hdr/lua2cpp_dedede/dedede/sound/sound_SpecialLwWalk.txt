#[acmd_script( agent = "dedede", script = "sound_speciallwwalk" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_speciallwwalk(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	wait_loop_sync_mot();
	frame(lua_state, 9.0);
	if is_excute(fighter) {
		PLAY_STEP(fighter, Hash40::new("se_dedede_step_right_m"));
	}
	frame(lua_state, 25.0);
}