#[acmd_script( agent = "dedede_star", script = "effect_fly", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_fly(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("dedede_superjump_star"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}