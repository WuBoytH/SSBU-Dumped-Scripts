#[acmd_script( agent = "brave_fireball", script = "sound_specialn1", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialn1(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        SET_TAKEOUT_SE(agent, Hash40::new("se_brave_special_n02"));
    }
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_brave_special_n02loop"));
    }
}