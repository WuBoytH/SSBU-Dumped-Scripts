#[acmd_script( agent = "brave_fireball", script = "game_burstm", category = ACMD_GAME, low_priority )]
unsafe fn game_burstm(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        QUAKE(fighter, *CAMERA_QUAKE_KIND_S);
        ControlModule::set_rumble(boma, Hash40::new("rbkind_explosion"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        notify_event_msc_cmd(0x199c462b5d);
    }
}