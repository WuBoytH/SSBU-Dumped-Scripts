#[acmd_script( agent = "brave_sleep", script = "effect_sleep", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_sleep(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("brave_sleep"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 0.7, false);
    }
}