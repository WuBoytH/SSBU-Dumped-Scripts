#[acmd_script( agent = "brave_explosion", script = "sound_explode1", category = ACMD_SOUND, low_priority )]
unsafe fn sound_explode1(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_brave_special_l03_03"));
    }
}