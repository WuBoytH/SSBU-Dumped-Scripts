#[acmd_script( agent = "brave", script = "sound_downattackd", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downattackd(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_brave_rise"));
    }
    frame(lua_state, 16.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_brave_attackhard_s02"));
    }
    frame(lua_state, 20.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_brave_attackair_f01"));
    }
    frame(lua_state, 59.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_brave_step_left_s"));
    }
}