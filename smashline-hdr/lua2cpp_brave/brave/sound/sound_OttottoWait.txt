#[acmd_script( agent = "brave", script = "sound_ottottowait", category = ACMD_SOUND, low_priority )]
unsafe fn sound_ottottowait(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_brave_rnd_ottotto"));
    }
}