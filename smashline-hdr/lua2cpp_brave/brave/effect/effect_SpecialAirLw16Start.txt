#[acmd_script( agent = "brave", script = "effect_specialairlw16start", category = ACMD_EFFECT, low_priority)]
unsafe fn effect_specialairlw16start(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        EFFECT_FOLLOW(fighter, Hash40::new("brave_flying"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
        EFFECT_DETACH_KIND(fighter, Hash40::new("brave_flying"), -1);
    }
}