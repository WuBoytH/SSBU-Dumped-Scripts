unsafe extern "C" fn effect_speciallw10end(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("brave_steel_release"), Hash40::new("rot"), 2, -4, -2, 0, 0, 0, 1, false);
    }
}