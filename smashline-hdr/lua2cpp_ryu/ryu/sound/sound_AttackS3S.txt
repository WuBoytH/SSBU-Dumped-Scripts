#[acmd_script( agent = "ryu", script = "sound_attacks3s", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacks3s(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_ryu_attack08"));
    }
    wait(lua_state, 4.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ryu_attackhard_s01"));
    }
}