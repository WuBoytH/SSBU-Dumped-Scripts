#[acmd_script( agent = "ryu", script = "sound_downattacku", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downattacku(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 12.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ryu_swing_kick_l"));
    }
    wait(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ryu_swing_kick_l"));
    }
}