#[acmd_script( agent = "ryu", script = "sound_downbackd", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downbackd(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ryu_escape"));
    }
    frame(lua_state, 24.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_ryu_landing01"));
    }
}