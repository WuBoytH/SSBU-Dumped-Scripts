#[acmd_script( agent = "ryu_hadoken", script = "effect_movespm", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_movespm(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("ryu_syakunetsu_bullet"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 0.65, false);
    }
}