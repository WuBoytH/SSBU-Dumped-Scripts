#[acmd_script( agent = "ryu_hadoken", script = "sound_movespm", category = ACMD_SOUND, low_priority )]
unsafe fn sound_movespm(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ryu_special_n02"));
    }
}