#[acmd_script( agent = "captain", script = "sound_turn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_turn(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 9.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_captain_leather_creak01"));
    }
}