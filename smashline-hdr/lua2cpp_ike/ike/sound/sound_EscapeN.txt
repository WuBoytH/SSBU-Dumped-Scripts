#[acmd_script( agent = "ike", script = "sound_escapen", category = ACMD_SOUND, low_priority )]
unsafe fn sound_escapen(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_ike_escapeair"));
    }
    wait(lua_state, 29.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_ike_step_right_m"));
    }
}