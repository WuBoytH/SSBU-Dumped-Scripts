#[acmd_script( agent = "jack", script = "sound_wait3", category = ACMD_SOUND, low_priority)]
unsafe fn sound_wait3(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 33.0);
    if is_excute(fighter) {
        PLAY_STATUS(fighter, Hash40::new("se_jack_wait04"));
    }
    frame(lua_state, 38.0);
    if is_excute(fighter) {
        PLAY_STATUS(fighter, Hash40::new("se_jack_wait03"));
    }
}