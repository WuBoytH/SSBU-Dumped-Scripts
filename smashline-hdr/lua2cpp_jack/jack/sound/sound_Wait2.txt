#[acmd_script( agent = "jack", script = "sound_wait2", category = ACMD_SOUND, low_priority )]
unsafe fn sound_wait2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 20.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_jack_wait01"));
    }
    frame(lua_state, 72.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_jack_wait02"));
    }
}