#[acmd_script( agent = "jack", script = "sound_specialhipull2", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialhipull2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_jack_special_h01"));
        PLAY_SE(agent, Hash40::new("se_common_cliff_catch"));
    }
}