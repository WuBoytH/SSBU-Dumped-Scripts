#[acmd_script( agent = "jack", script = "sound_speciallwdamage", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallwdamage(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_jack_special_l01"));
    }
}