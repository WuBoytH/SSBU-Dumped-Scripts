#[acmd_script( agent = "jack", script = "expression_wait4", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_wait4(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_LR, 5);
    }
    frame(lua_state, 20.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_46_flame"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}