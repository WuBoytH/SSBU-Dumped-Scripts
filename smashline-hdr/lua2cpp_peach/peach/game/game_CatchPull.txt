#[acmd_script( agent = "peach", script = "game_catchpull", category = ACMD_GAME, low_priority )]
unsafe fn game_catchpull(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    IS_EXIST_ARTICLE(fighter, *FIGHTER_PEACH_GENERATE_ARTICLE_KINOPIO);
    if(methodlib::L2CValue::operator==(lib::L2CValueconst&)const(false, true)){
        if is_excute(fighter) {
            ArticleModule::remove_exist(boma, *FIGHTER_PEACH_GENERATE_ARTICLE_KINOPIO);
        }
    }
    if is_excute(fighter) {
        ArticleModule::generate_article(boma, *FIGHTER_PEACH_GENERATE_ARTICLE_KINOPIO);
        methodlib::L2CValue::as_hash()const(*FIGHTER_PEACH_GENERATE_ARTICLE_KINOPIO, Hash40::new("catch_pull"));
        ArticleModule::change_motion(boma);
    }
}