#[acmd_script( agent = "peach", script = "game_specialhifall", category = ACMD_GAME, low_priority )]
unsafe fn game_specialhifall(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ArticleModule::change_motion(boma, *FIGHTER_PEACH_GENERATE_ARTICLE_KASSAR, Hash40::new("special_hi_fall"), false, -1.0);
    }
}