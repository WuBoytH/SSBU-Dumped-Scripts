#[acmd_script( agent = "peach", script = "sound_final", category = ACMD_SOUND, low_priority )]
unsafe fn sound_final(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_peach_final01"));
        SET_PLAY_INHIVIT(agent, Hash40::new("se_peach_final01"), 180);
    }
}