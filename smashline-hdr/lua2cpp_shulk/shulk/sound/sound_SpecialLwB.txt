#[acmd_script( agent = "shulk", script = "sound_speciallwb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallwb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_shulk_special_l02"));
    }
}