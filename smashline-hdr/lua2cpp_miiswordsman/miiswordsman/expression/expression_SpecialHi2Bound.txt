#[acmd_script( agent = "miiswordsman", script = "expression_specialhi2bound", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialhi2bound(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ItemModule::set_have_item_visibility(boma, false, 0);
        ControlModule::set_rumble(boma, Hash40::new("rbkind_impact"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}