#[acmd_script( agent = "miiswordsman", script = "expression_runfalll", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_runfalll(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        notify_event_msc_cmd!(agent, Hash40::new_raw(0x26769bd1de), 0, 30, 2);
    }
}