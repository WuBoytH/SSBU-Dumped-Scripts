#[acmd_script( agent = "miiswordsman", script = "expression_finalairhold", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_finalairhold(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        QUAKE(agent, *CAMERA_QUAKE_KIND_L);
    }
    frame(lua_state, 4.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_spinslash"), 220, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}