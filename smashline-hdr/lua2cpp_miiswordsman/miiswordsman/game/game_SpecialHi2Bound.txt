#[acmd_script( agent = "miiswordsman", script = "game_specialhi2bound", category = ACMD_GAME, low_priority )]
unsafe fn game_specialhi2bound(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        SET_AIR(agent);
    }
}