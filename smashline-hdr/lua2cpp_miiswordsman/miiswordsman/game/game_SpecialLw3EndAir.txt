#[acmd_script( agent = "miiswordsman", script = "game_speciallw3endair", category = ACMD_GAME, low_priority )]
unsafe fn game_speciallw3endair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait(lua_state, 8.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_MIISWORDSMAN_STATUS_WORK_ID_FLAG_JET_STUB_FALL_ONOFF);
    }
}