unsafe extern "C" fn sound_sleeploop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("vc_mii_furasleep"));
    }
    wait(lua_state, 220.0);
}