#[acmd_script( agent = "tantan", script = "game_catchdashreturn", category = ACMD_GAME, low_priority )]
unsafe fn game_catchdashreturn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area(boma, 3.5, 3.5);
    }
}