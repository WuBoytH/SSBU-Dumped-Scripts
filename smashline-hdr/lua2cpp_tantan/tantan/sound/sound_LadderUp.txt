#[acmd_script( agent = "tantan", script = "sound_ladderup", category = ACMD_SOUND, low_priority )]
unsafe fn sound_ladderup(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_ladderstep_m"));
    }
    wait(lua_state, 4.0);
}