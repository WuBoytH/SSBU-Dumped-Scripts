#[acmd_script( agent = "tantan", script = "sound_cliffescape", category = ACMD_SOUND, low_priority )]
unsafe fn sound_cliffescape(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 10.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_tantan_jump01"));
    }
    frame(lua_state, 18.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_tantan_escapecliff"));
    }
    frame(lua_state, 34.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_tantan_landing01"));
    }
}