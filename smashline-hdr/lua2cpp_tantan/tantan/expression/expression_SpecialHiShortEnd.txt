#[acmd_script( agent = "tantan", script = "expression_specialhishortend", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialhishortend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_attackss"), 4, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}