#[acmd_script( agent = "tantan", script = "expression_specialhishortend", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialhishortend(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_attackss"), 4, false);
        ControlModule::set_rumble(boma);
    }
}