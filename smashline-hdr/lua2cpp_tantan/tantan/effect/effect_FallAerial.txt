#[acmd_script( agent = "tantan", script = "effect_fallaerial", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_fallaerial(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("sys_jump_aerial"), true, true);
    }
}