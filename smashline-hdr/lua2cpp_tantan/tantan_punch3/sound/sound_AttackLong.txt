#[acmd_script( agent = "tantan_punch3", script = "sound_attacklong", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacklong(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_tantan_attack03_long"));
    }
}