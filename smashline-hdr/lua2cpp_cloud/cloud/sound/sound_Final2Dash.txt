#[acmd_script( agent = "cloud", script = "sound_final2dash", category = ACMD_SOUND, low_priority )]
unsafe fn sound_final2dash(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_cloud_final02"));
    }
}