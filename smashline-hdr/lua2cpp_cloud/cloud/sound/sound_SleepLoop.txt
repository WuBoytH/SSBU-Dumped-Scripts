#[acmd_script( agent = "cloud", script = "sound_sleeploop", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleeploop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 1.0);
    PLAY_STATUS(agent, Hash40::new("vc_cloud_furasleep"));
}