#[acmd_script( agent = "cloud_wave", script = "sound_regular_lb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_regular_lb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_cloud_special_n05"));
    }
}