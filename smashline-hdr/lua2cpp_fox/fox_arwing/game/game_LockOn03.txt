#[acmd_script( agent = "fox_arwing", script = "game_lockon03", category = ACMD_GAME, low_priority )]
unsafe fn game_lockon03(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 58.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *WEAPON_FOX_ARWING_STATUS_WORK_FLAG_SET_DEPTH_OFFSET);
    }
}