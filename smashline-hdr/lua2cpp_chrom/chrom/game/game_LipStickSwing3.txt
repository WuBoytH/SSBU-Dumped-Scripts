#[acmd_script( agent = "chrom", script = "game_lipstickswing3", category = ACMD_GAME, low_priority )]
unsafe fn game_lipstickswing3(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 8.0);
    methodlua2cpp::L2CFighterAnimcmdGameCommon::game_LipStickSwing3Common()();
    if is_excute(agent) {
        SHOOT_ITEM_BULLET_CHARGE(agent, 0);
    }
    frame(lua_state, 10.0);
    if is_excute(agent) {
        AttackModule::clear_all(boma);
    }
}