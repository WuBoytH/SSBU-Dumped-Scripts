#[acmd_script( agent = "chrom", script = "sound_specialairs3lw", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairs3lw(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_chrom_attack03"));
        PLAY_SE(agent, Hash40::new("se_chrom_special_s03l"));
    }
}