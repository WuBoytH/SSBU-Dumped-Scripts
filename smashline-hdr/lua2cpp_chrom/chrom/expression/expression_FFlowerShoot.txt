#[acmd_script( agent = "chrom", script = "expression_fflowershoot", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_fflowershoot(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        VisibilityModule::set_int64(boma, hash40("sword") as i64, hash40("sword_saya_tsuka") as i64);
        slope!(fighter, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
    }
}