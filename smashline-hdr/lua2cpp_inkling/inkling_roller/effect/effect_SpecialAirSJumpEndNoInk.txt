#[acmd_script( agent = "inkling_roller", script = "effect_specialairsjumpendnoink", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairsjumpendnoink(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("inkling_roller_splash_run"), false, true);
        EFFECT_OFF_KIND(agent, Hash40::new("inkling_roller_splash_walk"), false, true);
    }
}