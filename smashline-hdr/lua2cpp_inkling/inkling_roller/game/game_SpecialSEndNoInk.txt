#[acmd_script( agent = "inkling_roller", script = "game_specialsendnoink", category = ACMD_GAME, low_priority )]
unsafe fn game_specialsendnoink(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        AttackModule::clear_all(boma);
    }
    frame(lua_state, 16.0);
    if is_excute(agent) {
        notify_event_msc_cmd!(agent, Hash40::new_raw(0x199c462b5d));
    }
}