#[acmd_script( agent = "inkling", script = "expression_specialairlwempty", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairlwempty(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        VisibilityModule::set_int64(boma, hash40("tank") as i64, hash40("tank_subempty") as i64);
        ItemModule::set_have_item_visibility(boma, false, 0);
    }
}