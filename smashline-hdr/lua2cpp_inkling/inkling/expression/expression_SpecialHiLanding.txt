#[acmd_script( agent = "inkling", script = "expression_specialhilanding" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_specialhilanding(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_TOP);
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_dive"), 0, false);
        ControlModule::set_rumble(boma);
    }
    frame(lua_state, 10.0);
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_LR, 12);
    }
    frame(lua_state, 5.0);
    if is_excute(fighter) {
        InkPaintModule::set_special_paint(boma, *SPECIAL_PAINT_SQUID_TO_HUMAN);
    }
    frame(lua_state, 10.0);
    if is_excute(fighter) {
        InkPaintModule::set_special_paint(boma, *SPECIAL_PAINT_SQUID_TO_HUMAN);
    }
    frame(lua_state, 15.0);
    if is_excute(fighter) {
        InkPaintModule::set_special_paint(boma, *SPECIAL_PAINT_SQUID_TO_HUMAN);
    }
}