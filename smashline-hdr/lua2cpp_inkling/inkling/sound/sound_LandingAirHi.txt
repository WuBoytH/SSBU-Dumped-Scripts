#[acmd_script( agent = "inkling", script = "sound_landingairhi", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landingairhi(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_LANDING_SE(agent, Hash40::new("se_inkling_landing02"));
    }
}