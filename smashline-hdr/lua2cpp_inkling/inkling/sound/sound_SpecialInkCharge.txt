#[acmd_script( agent = "inkling", script = "sound_specialinkcharge", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialinkcharge(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_inkling_special_n05"));
    }
}