#[acmd_script( agent = "inkling", script = "sound_attackairlw", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackairlw(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 14.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_inkling_attackair_l01"));
        PLAY_SEQUENCE(agent, Hash40::new("seq_inkling_rnd_attack"));
    }
}