#[acmd_script( agent = "inkling", script = "game_specialairsdash", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairsdash(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FighterAreaModuleImpl::enable_fix_jostle_area(boma, 3.0, 8.0);
    }
}