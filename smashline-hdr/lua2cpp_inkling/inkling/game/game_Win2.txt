#[acmd_script( agent = "inkling", script = "game_win2" , category = ACMD_GAME , low_priority)]
unsafe fn game_win2(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_INKLING_GENERATE_ARTICLE_ROLLER);
        ArticleModule::remove_exist(boma, *FIGHTER_INKLING_GENERATE_ARTICLE_SLOSHER);
        ArticleModule::generate_article(boma, *FIGHTER_INKLING_GENERATE_ARTICLE_ROLLER);
        methodlib::L2CValue::as_hash()const(*FIGHTER_INKLING_GENERATE_ARTICLE_ROLLER, Hash40::new("win_2"));
        ArticleModule::change_motion(boma);
    }
}