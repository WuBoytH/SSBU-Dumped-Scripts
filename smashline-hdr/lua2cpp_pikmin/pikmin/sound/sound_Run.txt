unsafe extern "C" fn sound_run(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_pikmin_step_right_ll"));
    }
    wait(lua_state, 10.0);
    PLAY_STEP(agent, Hash40::new("se_pikmin_step_left_ll"));
}