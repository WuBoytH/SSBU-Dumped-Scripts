#[acmd_script( agent = "pikmin_dolfin", script = "effect_finalexplosion", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalexplosion(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EffectModule::req_screen(boma, Hash40::new("bg_pikmin_final2"), false, false, false);
        EFFECT(agent, Hash40::new("pikmin_final_bomb"), Hash40::new("body"), 0, 0, 0, 0, 0, 0, 2, 0, 0, 0, 0, 0, 0, false);
    }
    for _ in 0..30 {
    if is_excute(agent) {
        QUAKE(agent, *CAMERA_QUAKE_KIND_L);
    }
    wait(lua_state, 20.0);
}
}