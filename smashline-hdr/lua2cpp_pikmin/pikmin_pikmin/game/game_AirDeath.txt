#[acmd_script( agent = "pikmin_pikmin", script = "game_airdeath", category = ACMD_GAME, low_priority )]
unsafe fn game_airdeath(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        SET_SPEED_EX(agent, -0.4, 1.8);
    }
}