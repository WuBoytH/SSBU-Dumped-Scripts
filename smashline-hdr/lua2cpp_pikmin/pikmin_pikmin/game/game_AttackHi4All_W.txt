#[acmd_script( agent = "pikmin_pikmin", script = "game_attackhi4all_w", category = ACMD_GAME, low_priority )]
unsafe fn game_attackhi4all_w(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        AttackModule::clear_all(boma);
    }
}