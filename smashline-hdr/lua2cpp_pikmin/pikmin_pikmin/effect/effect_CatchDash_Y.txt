#[acmd_script( agent = "pikmin_pikmin", script = "effect_catchdash_y", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_catchdash_y(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("pikmin_grab_dash"), Hash40::new("top"), 0, 3, 0, 0, 0, 0, 1, true);
        LAST_EFFECT_SET_ALPHA(agent, 0.8);
    }
}