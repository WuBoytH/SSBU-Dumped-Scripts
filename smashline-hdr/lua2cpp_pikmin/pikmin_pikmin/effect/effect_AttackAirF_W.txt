#[acmd_script( agent = "pikmin_pikmin", script = "effect_attackairf_w", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_attackairf_w(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        EFFECT_FOLLOW_ALPHA(agent, Hash40::new("pikmin_attack_arc"), Hash40::new("top"), 3, 6, 3.5, 10, -10, -70, 1, true, 0.4);
    }
}