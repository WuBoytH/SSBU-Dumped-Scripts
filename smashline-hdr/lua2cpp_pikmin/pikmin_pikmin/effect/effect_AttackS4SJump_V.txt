#[acmd_script( agent = "pikmin_pikmin", script = "effect_attacks4sjump_v", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_attacks4sjump_v(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("pikmin_smash_trail"), Hash40::new("top"), 0, 4, 3, 0, 0, 0, 1, true);
        LAST_EFFECT_SET_COLOR(agent, 0.36, 0, 1);
        EFFECT_FOLLOW(agent, Hash40::new("pikmin_attack_flash"), Hash40::new("top"), 0, 4, 1.6, 0, 0, 0, 1, true);
        LAST_EFFECT_SET_COLOR(agent, 0.36, 0, 1);
    }
    for _ in 0..5 {
    if is_excute(agent) {
        FLASH(agent, 0.8, 1, 0.5, 0);
        FLASH_FRM(agent, 2, 0.8, 1, 0.5, 0.3);
    }
    wait(lua_state, 2.0);
    if is_excute(agent) {
        FLASH_FRM(agent, 2, 0.8, 1, 0.5, 0);
    }
    wait(lua_state, 2.0);
    if is_excute(agent) {
        COL_NORMAL(agent);
    }
}
}