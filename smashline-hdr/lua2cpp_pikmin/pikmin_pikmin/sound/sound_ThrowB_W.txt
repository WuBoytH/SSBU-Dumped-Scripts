#[acmd_script( agent = "pikmin_pikmin", script = "sound_throwb_w", category = ACMD_SOUND, low_priority )]
unsafe fn sound_throwb_w(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_swing_02"));
    }
}