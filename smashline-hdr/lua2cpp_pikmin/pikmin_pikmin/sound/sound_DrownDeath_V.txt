#[acmd_script( agent = "pikmin_pikmin", script = "sound_drowndeath_v", category = ACMD_SOUND, low_priority )]
unsafe fn sound_drowndeath_v(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pikmin_dead01"));
    }
    wait(lua_state, 32.0);
    if is_excute(agent) {
        PLAY_SE_REMAIN(agent, Hash40::new("se_pikmin_dead02"));
    }
}