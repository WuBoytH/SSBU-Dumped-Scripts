#[acmd_script( agent = "pzenigame", script = "game_specialsoverturnstartair", category = ACMD_GAME, low_priority )]
unsafe fn game_specialsoverturnstartair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 6.0);
    if is_excute(agent) {
        SET_SPEED_EX(agent, 0.7, -0.5, *KINETIC_ENERGY_RESERVE_ATTRIBUTE_MAIN);
    }
}