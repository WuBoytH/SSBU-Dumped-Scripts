#[acmd_script( agent = "pzenigame", script = "game_specialairnshot", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairnshot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_PZENIGAME_STATUS_SPECIAL_N_FLAG_SHOOT_ANGLE_ENABLE);
    }
    FT_MOTION_RATE(agent, 0.85);
    frame(lua_state, 7.0);
    for _ in 0..6 {
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_PZENIGAME_GENERATE_ARTICLE_WATER, false, -1);
    }
    wait(lua_state, 4.0);
}
frame(lua_state, 40.0);
if is_excute(agent) {
    WorkModule::off_flag(boma, *FIGHTER_PZENIGAME_STATUS_SPECIAL_N_FLAG_SHOOT_ANGLE_ENABLE);
}
}