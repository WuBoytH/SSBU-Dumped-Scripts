#[acmd_script( agent = "pzenigame", script = "sound_specialairshit", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairshit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pzenigame_special_s03"));
    }
}