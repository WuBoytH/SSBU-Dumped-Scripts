unsafe extern "C" fn sound_squatb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pzenigame_crawing_b02"));
    }
    frame(lua_state, 25.0);
    PLAY_STEP(agent, 0x18d857b668);
}