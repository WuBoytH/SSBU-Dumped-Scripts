#[acmd_script( agent = "wiifit_sunbullet", script = "effect_hold", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_hold(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("wiifit_taiyo_hold"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}