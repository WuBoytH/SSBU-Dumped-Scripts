#[acmd_script( agent = "lucario_lucariom", script = "sound_finaljump", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finaljump(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_lucario_final03"));
    }
}