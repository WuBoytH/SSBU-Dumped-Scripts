#[acmd_script( agent = "lucario", script = "sound_ottotto", category = ACMD_SOUND, low_priority )]
unsafe fn sound_ottotto(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SEQUENCE(agent, Hash40::new("seq_lucario_rnd_ottotto"));
    }
}