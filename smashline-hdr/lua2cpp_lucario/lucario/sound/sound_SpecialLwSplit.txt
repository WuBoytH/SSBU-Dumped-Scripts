#[acmd_script( agent = "lucario", script = "sound_speciallwsplit", category = ACMD_SOUND, low_priority )]
unsafe fn sound_speciallwsplit(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        PLAY_SE_REMAIN(fighter, Hash40::new("vc_lucario_007"));
    }
    wait(lua_state, 1.0);
    if is_excute(fighter) {
        PLAY_SE_REMAIN(fighter, Hash40::new("se_lucario_special_l02"));
    }
}