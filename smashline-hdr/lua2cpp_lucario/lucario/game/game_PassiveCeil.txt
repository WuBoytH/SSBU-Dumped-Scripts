#[acmd_script( agent = "lucario", script = "game_passiveceil", category = ACMD_GAME, low_priority )]
unsafe fn game_passiveceil(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        KineticModule::change_kinetic(boma, *FIGHTER_KINETIC_TYPE_PASSIVE_CEIL);
    }
}