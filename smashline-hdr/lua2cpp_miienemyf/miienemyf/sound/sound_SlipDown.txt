#[acmd_script( agent = "miienemyf", script = "sound_slipdown", category = ACMD_SOUND, low_priority )]
unsafe fn sound_slipdown(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_slip_01"));
    }
    wait(lua_state, 21.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_common_falldown_01"));
    }
}