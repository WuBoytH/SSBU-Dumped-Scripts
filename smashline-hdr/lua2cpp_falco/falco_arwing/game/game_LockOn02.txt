#[acmd_script( agent = "falco_arwing", script = "game_lockon02", category = ACMD_GAME, low_priority )]
unsafe fn game_lockon02(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 45.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *WEAPON_FALCO_ARWING_STATUS_WORK_FLAG_SET_DEPTH_OFFSET);
    }
}