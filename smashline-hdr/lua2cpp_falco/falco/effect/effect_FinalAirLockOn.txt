#[acmd_script( agent = "falco", script = "effect_finalairlockon", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalairlockon(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        FILL_SCREEN_arg9(0, 14, 0, 0.008, 0.012, 0.745, EffectScreenLayer:*GROUND, *EFFECT_SCREEN_PRIO_FINAL, EffectScreenBlendType:*BLEND);
    }
    frame(lua_state, 22.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("falco_final_lock_on"), Hash40::new("top"), 0, 13, 27, 0, 0, 0, 2.3, false);
    }
    frame(lua_state, 27.0);
    if is_excute(agent) {
        EFFECT_DETACH_KIND(agent, Hash40::new("falco_final_lock_on"), -1);
    }
}