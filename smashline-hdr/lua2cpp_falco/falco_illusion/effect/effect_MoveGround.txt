#[acmd_script( agent = "falco_illusion", script = "effect_moveground", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_moveground(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("falco_vision_m"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1, true);
    }
}