#[acmd_script( agent = "gamewatch", script = "expression_specialairlwend", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairlwend(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        ItemModule::set_attach_item_visibility(boma, false);
        ItemModule::set_have_item_visibility(boma, false);
        VisibilityModule::set_int64(boma, Hash40::new("head"), Hash40::new("head_none"));
    }
}