#[acmd_script( agent = "gamewatch", script = "expression_scopeairfire", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_scopeairfire(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        VisibilityModule::set_int64(boma, hash40("hand") as i64, hash40("hand_hold_l") as i64);
    }
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        QUAKE(fighter, *CAMERA_QUAKE_KIND_S);
    }
}