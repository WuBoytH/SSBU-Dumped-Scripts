#[acmd_script( agent = "gamewatch", script = "sound_specials3", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specials3(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 16.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_gamewatch_wave04_mi"));
    }
    wait(lua_state, 3.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_gamewatch_wave04_mi"));
    }
}