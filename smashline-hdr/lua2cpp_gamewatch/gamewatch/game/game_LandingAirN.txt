unsafe extern "C" fn game_landingairn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if IS_EXIST_ARTICLE(agent, *FIGHTER_GAMEWATCH_GENERATE_ARTICLE_NORMAL_WEAPON) {
        if is_excute(agent) {
            ArticleModule::change_motion(boma, *FIGHTER_GAMEWATCH_GENERATE_ARTICLE_NORMAL_WEAPON, Hash40::new("landing_air_n"), false, -1.0);
            get_value_float(*FIGHTER_GAMEWATCH_GENERATE_ARTICLE_NORMAL_WEAPON, *SO_VAR_FLOAT_MOTION_RATE_TOTAL);
            SET_RATE_ARTICLE_V(agent, 256);
        }
    }
}