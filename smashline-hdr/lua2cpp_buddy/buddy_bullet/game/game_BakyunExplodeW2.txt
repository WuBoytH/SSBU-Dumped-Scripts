#[acmd_script( agent = "buddy_bullet", script = "game_bakyunexplodew2", category = ACMD_GAME, low_priority )]
unsafe fn game_bakyunexplodew2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        VisibilityModule::set_whole(boma, false);
    }
    frame(lua_state, 1.0);
    if is_excute(agent) {
        notify_event_msc_cmd!(agent, Hash40::new_raw(0x199c462b5d));
    }
}