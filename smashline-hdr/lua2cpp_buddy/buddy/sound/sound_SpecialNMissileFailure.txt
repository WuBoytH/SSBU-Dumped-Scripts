#[acmd_script( agent = "buddy", script = "sound_specialnmissilefailure" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialnmissilefailure(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		PLAY_SE(fighter, Hash40::new("se_buddy_step_left_m"));
	}
}