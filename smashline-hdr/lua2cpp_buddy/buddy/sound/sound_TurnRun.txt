#[acmd_script( agent = "buddy", script = "sound_turnrun", category = ACMD_SOUND, low_priority )]
unsafe fn sound_turnrun(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 2.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_buddy_dash_turn"));
        SET_PLAY_INHIVIT(agent, Hash40::new("se_buddy_dash_turn"), 20);
    }
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_buddy_dash_turn_step_right"));
    }
    frame(lua_state, 10.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_buddy_dash_turn_step_left"));
    }
    frame(lua_state, 15.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_buddy_dash_turn_step_right"));
    }
}