#[acmd_script( agent = "buddy", script = "sound_downspot", category = ACMD_SOUND, low_priority )]
unsafe fn sound_downspot(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        PLAY_DOWN_SE(agent, Hash40::new("se_common_down_m_02"));
    }
    frame(lua_state, 21.0);
    if is_excute(agent) {
        PLAY_DOWN_SE(agent, Hash40::new("se_common_down_m_02"));
    }
}