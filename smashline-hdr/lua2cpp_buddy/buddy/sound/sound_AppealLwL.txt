#[acmd_script( agent = "buddy", script = "sound_appeallwl", category = ACMD_SOUND, low_priority)]
unsafe fn sound_appeallwl(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 1.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_buddy_appeal_l01"));
    }
    frame(lua_state, 8.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("vc_buddy_appeal03"));
    }
    frame(lua_state, 37.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_buddy_appeal_l02"));
    }
    frame(lua_state, 49.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("vc_buddy_appeal03"));
    }
}