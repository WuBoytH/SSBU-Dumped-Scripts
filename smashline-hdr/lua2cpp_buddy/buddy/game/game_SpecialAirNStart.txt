#[acmd_script( agent = "buddy", script = "game_specialairnstart", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairnstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    0x18cc20(*FIGHTER_INSTANCE_WORK_ID_INT_KIND, *FIGHTER_KIND_KIRBY);
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_BUDDY_GENERATE_ARTICLE_PARTNER, false, -1);
    }
    FT_MOTION_RATE(agent, 0.75);
    wait(lua_state, 36.0);
    FT_MOTION_RATE(agent, 0.25);
    frame(lua_state, 40.0);
    FT_MOTION_RATE(agent, 1.0);
}