#[acmd_script( agent = "lucina", script = "expression_swimturn", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_swimturn(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        VisibilityModule::set_int64(boma, hash40("sword") as i64, hash40("sword_saya") as i64);
    }
}