unsafe extern "C" fn sound_jumpsquat(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_daisy_dash_stop"));
        SET_PLAY_INHIVIT(agent, Hash40::new("se_daisy_dash_stop"), 15);
    }
}

unsafe extern "C" fn expression_jumpsquat(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_LR, 2);
    }
}