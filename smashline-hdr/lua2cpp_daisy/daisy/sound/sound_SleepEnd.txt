#[acmd_script( agent = "daisy", script = "sound_sleepend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleepend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("vc_daisy_wakeup"));
    }
}