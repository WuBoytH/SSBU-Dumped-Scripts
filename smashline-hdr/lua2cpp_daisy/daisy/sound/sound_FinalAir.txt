#[acmd_script( agent = "daisy", script = "sound_finalair" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_finalair(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	frame(lua_state, 1.0);
	if is_excute(fighter) {
		PLAY_SE(fighter, Hash40::new("se_daisy_final01"));
		SET_PLAY_INHIVIT(fighter, Hash40::new("se_daisy_final01"), 180);
	}
}