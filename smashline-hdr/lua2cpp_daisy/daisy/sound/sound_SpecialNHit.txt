#[acmd_script( agent = "daisy", script = "sound_specialnhit", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialnhit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_daisy_special_n03"));
    }
    frame(lua_state, 3.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_daisy_special_n01"));
        PLAY_SE(agent, Hash40::new("se_daisy_special_n02"));
    }
    frame(lua_state, 42.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_daisy_special_n04"));
    }
}