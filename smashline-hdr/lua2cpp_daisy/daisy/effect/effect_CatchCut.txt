#[acmd_script( agent = "daisy", script = "effect_catchcut", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_catchcut(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if sv_animcmd::get_value_float(agent, *SO_VAR_FLOAT_LR) < 0.0 {
        frame(lua_state, 16.0);
        if is_excute(agent) {
            EFFECT(agent, Hash40::new("sys_erace_smoke"), Hash40::new("top"), 3, 4, 0, 0, 0, 0, 0.9, 0, 0, 0, 0, 0, 0, true);
            LAST_EFFECT_SET_RATE(agent, 1.2);
        }
        else{
        frame(lua_state, 0.0);
        if is_excute(agent) {
            EFFECT(agent, Hash40::new("sys_erace_smoke"), Hash40::new("top"), -3, 4, 0, 0, 0, 0, 0.9, 0, 0, 0, 0, 0, 0, true);
            LAST_EFFECT_SET_RATE(agent, 1.2);
        }
    }
}
}