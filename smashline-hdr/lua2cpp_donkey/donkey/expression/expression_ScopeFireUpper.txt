#[acmd_script( agent = "donkey", script = "expression_scopefireupper" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_scopefireupper(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	frame(lua_state, 3.0);
	if is_excute(fighter) {
		QUAKE(fighter, *CAMERA_QUAKE_KIND_S);
	}
}