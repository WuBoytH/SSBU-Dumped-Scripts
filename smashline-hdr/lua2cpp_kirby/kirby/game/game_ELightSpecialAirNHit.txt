#[acmd_script( agent = "kirby", script = "game_elightspecialairnhit", category = ACMD_GAME, low_priority )]
unsafe fn game_elightspecialairnhit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        CLR_SPEED(agent, *FIGHTER_KINETIC_ENERGY_ID_GRAVITY);
        KineticModule::add_speed(boma, &Vector3f{x: 0.0, y: 0.4, z: 0.0});
    }
}