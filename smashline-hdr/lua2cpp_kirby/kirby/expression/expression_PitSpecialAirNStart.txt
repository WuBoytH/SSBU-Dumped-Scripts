#[acmd_script( agent = "kirby", script = "expression_pitspecialairnstart" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_pitspecialairnstart(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		slope(*MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
		ItemModule::set_have_item_visibility(boma, false);
	}
	frame(lua_state, 2.0);
	if is_excute(fighter) {
		ArticleModule::set_visibility_whole(boma, *FIGHTER_PIT_GENERATE_ARTICLE_BOW, true);
	}
}