#[acmd_script( agent = "kirby", script = "expression_wariospecialairnbiteend" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_wariospecialairnbiteend(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 11.0);
    if is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_attackm"), 0, false);
        ControlModule::set_rumble(boma);
    }
    frame(lua_state, 19.0);
    if is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
    }
}