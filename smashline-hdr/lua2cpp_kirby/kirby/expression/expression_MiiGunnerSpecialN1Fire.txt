#[acmd_script( agent = "kirby", script = "expression_miigunnerspecialn1fire", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_miigunnerspecialn1fire(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        ControlModule::set_rumble(boma, Hash40::new("rbkind_beams"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}