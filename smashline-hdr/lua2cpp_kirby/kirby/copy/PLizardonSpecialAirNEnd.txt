unsafe extern "C" fn effect_plizardonspecialairnend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("plizardon_kaenhousya_start"), false, false);
        EFFECT_OFF_KIND(agent, Hash40::new("plizardon_kaenhousya_start_air"), false, false);
        COL_NORMAL(agent);
    }
}

unsafe extern "C" fn expression_plizardonspecialairnend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
        ControlModule::set_rumble(boma, Hash40::new("rbkind_erase"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}