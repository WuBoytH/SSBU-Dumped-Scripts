#[acmd_script( agent = "kirby", script = "sound_pikminspecialn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_pikminspecialn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 3.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pikmin_special_n01"));
    }
    wait(lua_state, 19.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_pikmin_special_s01"));
    }
}