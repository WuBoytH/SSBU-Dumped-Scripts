#[acmd_script( agent = "kirby", script = "sound_packunspecialairnfailure", category = ACMD_SOUND, low_priority )]
unsafe fn sound_packunspecialairnfailure(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 25.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_packun_special_n06"));
    }
}