#[acmd_script( agent = "kirby", script = "sound_kirbyyounglinkbowchargemax", category = ACMD_SOUND, low_priority )]
unsafe fn sound_kirbyyounglinkbowchargemax(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_younglink_special_n04"));
    }
}