#[acmd_script( agent = "kirby", script = "sound_eflamespecialairnstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_eflamespecialairnstart(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_eflame_special_nstart"));
    }
}