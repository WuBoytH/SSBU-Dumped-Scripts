#[acmd_script( agent = "kirby", script = "sound_sheikspecialnloop", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sheikspecialnloop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 1.0);
    PLAY_SE(agent, 0x14d690ec1c);
}