#[acmd_script( agent = "kirby", script = "sound_miiswordsmanspecialairn1", category = ACMD_SOUND, low_priority)]
unsafe fn sound_miiswordsmanspecialairn1(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 3.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_common_swing_05"));
    }
    wait(lua_state, 9.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_common_swing_05"));
    }
    wait(lua_state, 8.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("vc_kirby_copy_mii_07"));
    }
}