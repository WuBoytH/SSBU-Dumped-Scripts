#[acmd_script( agent = "kirby", script = "sound_chromspecialnendmax", category = ACMD_SOUND, low_priority )]
unsafe fn sound_chromspecialnendmax(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 12.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_chrom_special_n05"));
        PLAY_SE(agent, Hash40::new("vc_kirby_copy_chrom_01"));
    }
}