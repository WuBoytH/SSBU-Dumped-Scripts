#[acmd_script( agent = "kirby", script = "sound_snakespecialairnthrowm", category = ACMD_SOUND, low_priority )]
unsafe fn sound_snakespecialairnthrowm(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 7.0);
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("se_snake_special_n02"));
    }
}