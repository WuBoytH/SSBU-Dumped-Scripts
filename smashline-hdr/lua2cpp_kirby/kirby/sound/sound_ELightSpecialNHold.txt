#[acmd_script( agent = "kirby", script = "sound_elightspecialnhold", category = ACMD_SOUND, low_priority)]
unsafe fn sound_elightspecialnhold(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        PLAY_STATUS(fighter, Hash40::new("se_elight_special_n01_hold"));
    }
}