#[acmd_script( agent = "kirby", script = "sound_miifighterspecialn1", category = ACMD_SOUND, low_priority )]
unsafe fn sound_miifighterspecialn1(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 4.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_miifighter_special_n01"));
    }
    wait(lua_state, 20.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("se_miifighter_special_n02"));
        PLAY_SE(fighter, Hash40::new("vc_kirby_copy_mii_01"));
    }
}