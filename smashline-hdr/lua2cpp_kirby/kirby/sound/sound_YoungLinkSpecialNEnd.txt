#[acmd_script( agent = "kirby", script = "sound_younglinkspecialnend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_younglinkspecialnend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_younglink_special_n01"));
    }
    frame(lua_state, 1.0);
    if WorkModule::is_flag(boma, *FIGHTER_LINK_STATUS_BOW_FLAG_CHARGE_MAX) {
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_younglink_special_n07"));
        }
        else{
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("se_younglink_special_n03"));
        }
    }
}
}