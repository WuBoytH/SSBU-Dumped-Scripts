#[acmd_script( agent = "duckhunt", script = "game_guarddamage", category = ACMD_GAME, low_priority )]
unsafe fn game_guarddamage(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        HIT_NODE(agent, Hash40::new("duckhip"), *HIT_STATUS_XLU);
    }
    frame(lua_state, 21.0);
    if is_excute(agent) {
        HIT_NODE(agent, Hash40::new("duckhip"), *HIT_STATUS_NORMAL);
    }
}