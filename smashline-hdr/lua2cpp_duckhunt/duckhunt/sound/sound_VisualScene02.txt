#[acmd_script( agent = "duckhunt", script = "sound_visualscene02", category = ACMD_SOUND, low_priority )]
unsafe fn sound_visualscene02(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 46.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_duckhunt_final03"));
    }
}