#[acmd_script( agent = "younglink", script = "game_appealsr", category = ACMD_GAME, low_priority )]
unsafe fn game_appealsr(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 2.0);
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_YOUNGLINK_GENERATE_ARTICLE_NAVY, ArticleOperationTarget(*ARTICLE_OPE_TARGET_ALL));
        ArticleModule::generate_article(boma, *FIGHTER_YOUNGLINK_GENERATE_ARTICLE_NAVY, true);
        ArticleModule::change_motion(boma, *FIGHTER_YOUNGLINK_GENERATE_ARTICLE_NAVY, Hash40::new("appeal_s_r"), false, -1.0);
    }
    frame(lua_state, 141.0);
    if is_excute(fighter) {
        ArticleModule::remove_exist(boma, *FIGHTER_YOUNGLINK_GENERATE_ARTICLE_NAVY, ArticleOperationTarget(*ARTICLE_OPE_TARGET_ALL));
    }
}