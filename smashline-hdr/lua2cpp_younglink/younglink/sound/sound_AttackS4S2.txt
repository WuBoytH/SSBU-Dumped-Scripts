#[acmd_script( agent = "younglink", script = "sound_attacks4s2" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_attacks4s2(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    frame(lua_state, 11.0);
    if is_excute(fighter) {
        PLAY_SE(fighter, Hash40::new("vc_younglink_attack03"));
        PLAY_SE(fighter, Hash40::new("se_common_smashswing_03"));
        PLAY_SE(fighter, Hash40::new("se_younglink_swing_ll"));
    }
}