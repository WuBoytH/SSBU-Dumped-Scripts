#[acmd_script( agent = "krool_ironball", script = "effect_hop", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_hop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("krool_cannon_bullet"), true, true);
    }
}