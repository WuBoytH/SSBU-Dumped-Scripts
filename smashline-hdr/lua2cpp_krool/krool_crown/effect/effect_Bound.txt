#[acmd_script( agent = "krool_crown", script = "effect_bound", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_bound(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT(agent, Hash40::new("sys_down_smoke"), Hash40::new("top"), 0, -3, 0, 0, 0, 0, 0.4, 0, 0, 0, 0, 0, 0, true);
    }
}