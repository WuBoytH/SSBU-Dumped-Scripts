#[acmd_script( agent = "krool_spitball", script = "effect_fly" , category = ACMD_EFFECT , low_priority)]
unsafe fn effect_fly(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
	if is_excute(fighter) {
		EFFECT_OFF_KIND(fighter, Hash40::new("krool_cannon_shot"), true, true);
	}
}