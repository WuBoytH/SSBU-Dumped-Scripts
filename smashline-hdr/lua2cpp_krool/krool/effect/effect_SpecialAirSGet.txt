#[acmd_script( agent = "krool", script = "effect_specialairsget", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialairsget(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("sys_item_get"), Hash40::new("handl"), 2, 0, 0, 0, 0, 0, 1, true);
    }
}