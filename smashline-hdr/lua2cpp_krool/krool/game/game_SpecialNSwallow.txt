#[acmd_script( agent = "krool", script = "game_specialnswallow", category = ACMD_GAME, low_priority )]
unsafe fn game_specialnswallow(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    frame(lua_state, 1.0);
    if is_excute(fighter) {
        MotionModule::set_rate(boma, 0.5);
    }
    frame(lua_state, 5.0);
    if is_excute(fighter) {
        WorkModule::on_flag(boma, *FIGHTER_KROOL_STATUS_SPECIAL_N_FLAG_SWALLOW_COMP);
        MotionModule::set_rate(boma, 1);
    }
}