#[acmd_script( agent = "krool", script = "sound_finaldashend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finaldashend(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 10.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_krool_final10"));
    }
}