#[acmd_script( agent = "krool", script = "sound_downdamageu3" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_downdamageu3(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    frame(lua_state, 22.0);
    if is_excute(fighter) {
        PLAY_DOWN_SE(fighter, Hash40::new("se_common_down_l_01"));
    }
    frame(lua_state, 38.0);
    if is_excute(fighter) {
        PLAY_DOWN_SE(fighter, Hash40::new("se_common_down_l_02"));
    }
}