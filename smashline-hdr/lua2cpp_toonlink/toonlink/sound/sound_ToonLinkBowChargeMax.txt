#[acmd_script( agent = "toonlink", script = "sound_toonlinkbowchargemax", category = ACMD_SOUND, low_priority )]
unsafe fn sound_toonlinkbowchargemax(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_toonlink_special_n04"));
    }
}