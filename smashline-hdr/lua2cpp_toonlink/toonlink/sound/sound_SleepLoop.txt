#[acmd_script( agent = "toonlink", script = "sound_sleeploop", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleeploop(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    if is_excute(agent) {
        PLAY_STATUS(agent, Hash40::new("vc_toonlink_furasleep"));
    }
    wait(lua_state, 1.0);
}