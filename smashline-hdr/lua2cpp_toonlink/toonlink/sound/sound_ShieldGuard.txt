#[acmd_script( agent = "toonlink", script = "sound_shieldguard", category = ACMD_SOUND, low_priority )]
unsafe fn sound_shieldguard(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_toonlink_sheildguard"));
    }
}