#[acmd_script( agent = "toonlink", script = "game_win1" , category = ACMD_GAME , low_priority)]
unsafe fn game_win1(fighter: &mut L2CAgentBase) {
	let lua_state = fighter.lua_state_agent;
	let boma = fighter.boma();
    if is_excute(fighter) {
        ArticleModule::generate_article(boma, *FIGHTER_TOONLINK_GENERATE_ARTICLE_TAKT);
        methodlib::L2CValue::as_hash()const(*FIGHTER_TOONLINK_GENERATE_ARTICLE_TAKT, Hash40::new("win1"));
        ArticleModule::change_motion(boma);
    }
}