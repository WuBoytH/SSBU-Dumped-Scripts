#[acmd_script( agent = "toonlink", script = "game_specialairs1", category = ACMD_GAME, low_priority )]
unsafe fn game_specialairs1(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 4.0);
    if is_excute(agent) {
        ArticleModule::generate_article(boma, *FIGHTER_TOONLINK_GENERATE_ARTICLE_BOOMERANG, false, -1);
    }
    frame(lua_state, 27.0);
    if is_excute(agent) {
        ArticleModule::shoot(boma, *FIGHTER_TOONLINK_GENERATE_ARTICLE_BOOMERANG, *ARTICLE_OPE_TARGET_ALL, false);
    }
}