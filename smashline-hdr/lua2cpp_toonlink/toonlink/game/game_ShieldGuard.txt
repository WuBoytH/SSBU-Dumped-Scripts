#[acmd_script( agent = "toonlink", script = "game_shieldguard", category = ACMD_GAME, low_priority )]
unsafe fn game_shieldguard(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_LINK_INSTANCE_WORK_ID_FLAG_WAIT_SHIELD);
    }
}