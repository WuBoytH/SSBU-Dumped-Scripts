unsafe extern "C" fn effect_specialairn(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("toonlink_arrow_max"), Hash40::new("handr"), 3, 0, 0, 0, 0, 0, 1, true);
    }
    wait(lua_state, 8.0);
}