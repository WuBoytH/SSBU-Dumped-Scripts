#[acmd_script( agent = "toonlink", script = "effect_win1wait", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_win1wait(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        EFFECT_OFF_KIND(agent, Hash40::new("toonlink_appeal_twinkle"), false, false);
    }
}