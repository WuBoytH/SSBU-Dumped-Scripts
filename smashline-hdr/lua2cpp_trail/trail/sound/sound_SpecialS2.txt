#[acmd_script( agent = "trail", script = "sound_specials2", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specials2(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_trail_special_s03"));
    }
}