#[acmd_script( agent = "trail", script = "game_clubswing4charge", category = ACMD_GAME, low_priority )]
unsafe fn game_clubswing4charge(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        damage!(agent, *MA_MSC_DAMAGE_DAMAGE_NO_REACTION, *DAMAGE_NO_REACTION_MODE_ALWAYS, 0);
    }
}