#[acmd_script( agent = "metaknight", script = "game_jumpaerialf5", category = ACMD_GAME, low_priority )]
unsafe fn game_jumpaerialf5(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 25.0);
    if is_excute(agent) {
        WorkModule::on_flag(boma, *FIGHTER_STATUS_JUMP_FLY_NEXT);
    }
}