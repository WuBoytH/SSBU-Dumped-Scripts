unsafe extern "C" fn sound_finalhit(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_simon_final02"));
    }
    frame(lua_state, 140.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_simon_final03"));
    }
}