#[acmd_script( agent = "simon", script = "sound_attacklw32landing", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attacklw32landing(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_simon_attackhard_l02"));
        PLAY_SE(agent, Hash40::new("se_simon_landing01"));
    }
}