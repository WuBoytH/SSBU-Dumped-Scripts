#[acmd_script( agent = "palutena", script = "game_finalairbeam", category = ACMD_GAME, low_priority )]
unsafe fn game_finalairbeam(fighter: &mut L2CAgentBase) {
    let lua_state = fighter.lua_state_agent;
    let boma = fighter.boma();
    if is_excute(fighter) {
        camera!(fighter, *MA_MSC_CMD_CAMERA_CAM_RECT, 25, -10, 10, -15);
    }
}