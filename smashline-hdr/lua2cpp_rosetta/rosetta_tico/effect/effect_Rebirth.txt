#[acmd_script( agent = "rosetta_tico", script = "effect_rebirth", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_rebirth(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    if !FighterManager::is_homerun_versus(singletons::FighterManager()) {
        if is_excute(agent) {
            EFFECT_FOLLOW(agent, Hash40::new("rosetta_tico_birth"), Hash40::new("trans"), 0, 1, 0, 0, 0, 0, 1, false);
        }
    }
}