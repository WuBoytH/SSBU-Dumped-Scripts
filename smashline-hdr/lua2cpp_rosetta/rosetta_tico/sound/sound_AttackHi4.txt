#[acmd_script( agent = "rosetta_tico", script = "sound_attackhi4", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackhi4(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 8.0);
    if is_excute(agent) {
        PLAY_SE(agent, Hash40::new("se_tico_smash_h01"));
    }
    if IS_RANDOM(agent, 4) {
        if is_excute(agent) {
            PLAY_SE(agent, Hash40::new("vc_tico_angry"));
        }
    }
}