#[acmd_script( agent = "rosetta_powerstar", script = "sound_end", category = ACMD_SOUND, low_priority )]
unsafe fn sound_end(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 1.0);
    if is_excute(agent) {
        STOP_SE(agent, Hash40::new("se_rosetta_final01"));
        PLAY_SE_REMAIN(agent, Hash40::new("se_common_bomb_ll"));
    }
}