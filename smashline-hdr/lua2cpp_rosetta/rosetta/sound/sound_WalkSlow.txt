#[acmd_script( agent = "rosetta", script = "sound_walkslow", category = ACMD_SOUND, low_priority )]
unsafe fn sound_walkslow(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 22.0);
    PLAY_STATUS(agent, 0x17b9aad280);
}