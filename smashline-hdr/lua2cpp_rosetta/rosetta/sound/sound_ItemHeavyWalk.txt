#[acmd_script( agent = "rosetta", script = "sound_itemheavywalk", category = ACMD_SOUND, low_priority )]
unsafe fn sound_itemheavywalk(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 17.0);
    if is_excute(agent) {
        PLAY_STEP(agent, Hash40::new("se_rosetta_step_right_m"));
    }
    frame(lua_state, 33.5);
    PLAY_STEP(agent, Hash40::new("se_rosetta_step_left_m"));
}