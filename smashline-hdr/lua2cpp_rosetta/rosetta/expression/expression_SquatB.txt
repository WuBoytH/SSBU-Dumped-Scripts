#[acmd_script( agent = "rosetta", script = "expression_squatb", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_squatb(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    wait_loop_sync_mot();
    frame(lua_state, 8.0);
    ControlModule::set_rumble(boma, Hash40::new("rbkind_floating"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
}