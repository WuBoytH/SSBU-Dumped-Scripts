#[acmd_script( agent = "wolf", script = "effect_specialhiholdair", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_specialhiholdair(agent: &mut L2CAgentBase) {
    let lua_state = agent.lua_state_agent;
    let boma = agent.boma();
    frame(lua_state, 5.0);
    if is_excute(agent) {
        EFFECT_FOLLOW(agent, Hash40::new("wolf_shoot_hold"), Hash40::new("top"), 0, 7, 0, 0, 0, 0, 1, true);
    }
    for _ in 0..4 {
    if is_excute(agent) {
        FLASH(agent, 0.5, 0.4, 1, 0.7);
    }
    wait(lua_state, 1.0);
    if is_excute(agent) {
        FLASH_FRM(agent, 2, 0.3, 0, 1, 0);
    }
    wait(lua_state, 2.0);
    if is_excute(agent) {
        COL_NORMAL(agent);
    }
    wait(lua_state, 1.0);
}
}