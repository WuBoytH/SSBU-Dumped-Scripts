#[acmd_script( agent = "jack", script = "effect_visualscene", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_visualscene(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        EffectModule::remove_screen(fighter.module_accessor, Hash40::new("bg_jack_final"), -1);
        macros::CANCEL_FILL_SCREEN(fighter, 0, 0);
        macros::CANCEL_FILL_SCREEN(fighter, 1, 0);
    }
}