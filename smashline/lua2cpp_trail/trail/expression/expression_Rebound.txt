#[acmd_script( agent = "trail", script = "expression_rebound", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_rebound(fighter: &mut L2CAgentBase) {
    methodlua2cpp::L2CFighterAnimcmdExpressionCommon::expression_ReboundRumble()();
    if macros::is_excute(fighter) {
        slope!(fighter, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_LR);
    }
    frame(fighter.lua_state_agent, 20.0);
    if ItemModule::is_have_item(fighter.module_accessor, 0) {
        if macros::is_excute(fighter) {
            macros::FT_MOTION_INTP_WAIT_ITEM(fighter, true, 0);
        }
    }
}