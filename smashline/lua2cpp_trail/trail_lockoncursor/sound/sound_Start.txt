#[acmd_script( agent = "trail_lockoncursor", script = "sound_start", category = ACMD_SOUND, low_priority )]
unsafe fn sound_start(agent: &mut L2CAgentBase) {
    frame(agent.lua_state_agent, 1.0);
    if macros::is_excute(agent) {
        macros::PLAY_SE(agent, Hash40::new("se_trail_special_s05"));
    }
}