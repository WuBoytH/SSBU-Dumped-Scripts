#[acmd_script( agent = "trail_lockoncursor", script = "sound_start" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_start(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_trail_special_s05"));
    }
}