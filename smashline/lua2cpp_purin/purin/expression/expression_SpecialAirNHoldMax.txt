#[acmd_script( agent = "purin", script = "expression_specialairnholdmax" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_specialairnholdmax(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    if macros::is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_smashhold2"), 0, true);
        ControlModule::set_rumble(fighter.module_accessor);
    }
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        ItemModule::set_have_item_visibility(fighter.module_accessor, false);
    }
    wait(fighter.lua_state_agent, 1.0);
}