#[acmd_script( agent = "packun_poisonbreath", script = "sound_shootmax", category = ACMD_SOUND, low_priority)]
unsafe fn sound_shootmax(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_STATUS(fighter, Hash40::new("se_packun_special_s04_max"));
    }
}