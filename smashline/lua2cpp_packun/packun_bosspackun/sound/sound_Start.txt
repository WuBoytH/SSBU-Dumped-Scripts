#[acmd_script( agent = "packun_bosspackun", script = "sound_start" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_start(fighter: &mut L2CAgentBase) {
	frame(fighter.lua_state_agent, 2.0);
	if macros::is_excute(fighter) {
		macros::PLAY_SE(fighter, Hash40::new("se_packun_final01"));
	}
