#[acmd_script( agent = "packun", script = "expression_specialairnblow", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairnblow(agent: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(agent.lua_state_agent, 1.0);
    for _ in 0..9 {
    if macros::is_excute(agent) {
        ControlModule::set_rumble(agent.module_accessor, Hash40::new("rbkind_elecattacks"), 0, true, *BATTLE_OBJECT_ID_INVALID as u32);
    }
    wait(agent.lua_state_agent, 14.0);
}
}