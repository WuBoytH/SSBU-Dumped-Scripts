#[acmd_script( agent = "sonic", script = "sound_specialairsend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairsend(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        sound!(agent, *MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}