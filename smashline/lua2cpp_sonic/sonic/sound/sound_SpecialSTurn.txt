#[acmd_script( agent = "sonic", script = "sound_specialsturn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialsturn(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_STATUS(fighter, Hash40::new("se_sonic_cliffclimb"));
    }
}