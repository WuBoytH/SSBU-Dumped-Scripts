#[acmd_script( agent = "edge", script = "expression_wingstart", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_wingstart(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        ControlModule::set_rumble(fighter.module_accessor, Hash40::new("rbkind_78_wingstart"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}