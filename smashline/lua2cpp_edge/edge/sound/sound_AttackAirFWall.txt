#[acmd_script( agent = "edge", script = "sound_attackairfwall", category = ACMD_SOUND, low_priority )]
unsafe fn sound_attackairfwall(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 2.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_edge_attackair_f03"));
    }
}