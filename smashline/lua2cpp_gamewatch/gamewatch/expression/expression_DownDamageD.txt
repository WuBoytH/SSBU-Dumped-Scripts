#[acmd_script( agent = "gamewatch", script = "expression_downdamaged", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_downdamaged(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_TOP);
    }
}