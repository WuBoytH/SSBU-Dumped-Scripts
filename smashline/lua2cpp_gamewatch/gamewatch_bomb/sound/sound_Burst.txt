#[acmd_script( agent = "gamewatch_bomb", script = "sound_burst" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_burst(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		macros::PLAY_SE(fighter, Hash40::new("se_common_bomb_l"));
	}
