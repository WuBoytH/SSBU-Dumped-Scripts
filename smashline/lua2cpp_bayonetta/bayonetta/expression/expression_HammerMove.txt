#[acmd_script( agent = "bayonetta", script = "expression_hammermove", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_hammermove(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(fighter.lua_state_agent, 1.0);
    execute(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        VisibilityModule::set_int64(fighter.module_accessor, hash40("gun_hand") as i64, hash40("gun_hand_hide_all") as i64);
    }
    frame(fighter.lua_state_agent, 4.0);
}