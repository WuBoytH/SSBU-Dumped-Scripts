#[acmd_script( agent = "bayonetta", script = "sound_finalend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_finalend(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        sound(*MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}