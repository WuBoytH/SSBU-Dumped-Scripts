#[acmd_script( agent = "reflet", script = "game_finalready" , category = ACMD_GAME , low_priority)]
unsafe fn game_finalready(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::WHOLE_HIT(fighter, *HIT_STATUS_XLU);
    }
}