#[acmd_script( agent = "toonlink", script = "sound_shieldguard", category = ACMD_SOUND, low_priority)]
unsafe fn sound_shieldguard(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_toonlink_sheildguard"));
    }
}