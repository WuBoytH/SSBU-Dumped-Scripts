#[acmd_script( agent = "toonlink", script = "sound_specialairn", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialairn(agent: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(agent.lua_state_agent, 0.0);
    if macros::is_excute(agent) {
        macros::PLAY_SE(agent, Hash40::new("se_toonlink_special_n02"));
    }
    wait(agent.lua_state_agent, 1.0);
}