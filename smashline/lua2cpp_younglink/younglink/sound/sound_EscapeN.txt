#[acmd_script( agent = "younglink", script = "sound_escapen", category = ACMD_SOUND, low_priority)]
unsafe fn sound_escapen(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_younglink_escapeair"));
    }
    wait(fighter.lua_state_agent, 29.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_younglink_landing01"));
    }
}