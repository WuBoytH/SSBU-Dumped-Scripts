#[acmd_script( agent = "pikmin_pikmin", script = "game_airdeath_v", category = ACMD_GAME, low_priority)]
unsafe fn game_airdeath_v(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::SET_SPEED_EX(fighter, -0.8, 1.2);
    }
}