#[acmd_script( agent = "koopajr_shadowmario", script = "sound_final", category = ACMD_SOUND, low_priority )]
unsafe fn sound_final(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 2.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE_REMAIN(fighter, Hash40::new("se_koopajr_final04"));
    }
    frame(fighter.lua_state_agent, 60.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE_REMAIN(fighter, Hash40::new("se_koopajr_final05"));
    }
}