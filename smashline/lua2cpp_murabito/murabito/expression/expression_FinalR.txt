#[acmd_script( agent = "murabito", script = "expression_finalr" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_finalr(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::START_INFO_FLASH_EYE(fighter);
        ItemModule::set_have_item_visibility(fighter.module_accessor, false);
    }
}