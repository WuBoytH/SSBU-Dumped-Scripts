#[acmd_script( agent = "richter_coffin", script = "sound_finalhit", category = ACMD_SOUND, low_priority)]
unsafe fn sound_finalhit(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_richter_final02"));
    }
    frame(fighter.lua_state_agent, 140.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_richter_final03"));
    }
}