#[acmd_script( agent = "richter", script = "sound_aircatchhang", category = ACMD_SOUND, low_priority )]
unsafe fn sound_aircatchhang(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_richter_attackair_h01"));
    }
}