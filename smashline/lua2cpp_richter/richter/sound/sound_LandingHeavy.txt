#[acmd_script( agent = "richter", script = "sound_landingheavy", category = ACMD_SOUND, low_priority )]
unsafe fn sound_landingheavy(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 2.0);
    if macros::is_excute(fighter) {
        macros::PLAY_LANDING_SE(fighter, Hash40::new("se_richter_landing02"));
        macros::PLAY_SE(fighter, Hash40::new("se_richter_whip_chain"));
    }
}