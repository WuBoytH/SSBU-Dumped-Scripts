#[acmd_script( agent = "wario_wariobike", script = "game_specialsbump", category = ACMD_GAME, low_priority )]
unsafe fn game_specialsbump(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        AttackModule::clear_all(fighter.module_accessor);
        WorkModule::on_flag(fighter.module_accessor, *WEAPON_WARIO_WARIOBIKE_STATUS_WORK_FLAG_BUMP_WARIO_AWAY);
    }
}