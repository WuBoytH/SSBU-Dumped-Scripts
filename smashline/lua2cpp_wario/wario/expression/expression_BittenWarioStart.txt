#[acmd_script( agent = "wario", script = "expression_bittenwariostart" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_bittenwariostart(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		ItemModule::set_have_item_visibility(fighter.module_accessor, false);
	}
