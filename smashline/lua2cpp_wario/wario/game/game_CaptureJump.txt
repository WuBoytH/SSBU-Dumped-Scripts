#[acmd_script( agent = "wario", script = "game_capturejump", category = ACMD_GAME, low_priority )]
unsafe fn game_capturejump(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        KineticModule::add_speed(fighter.module_accessor, &Vector3f{x: -0.6, y: 0.0, z: 0.0});
    }
}