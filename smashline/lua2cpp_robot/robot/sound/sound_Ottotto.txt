#[acmd_script( agent = "robot", script = "sound_ottotto", category = ACMD_SOUND, low_priority )]
unsafe fn sound_ottotto(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        macros::PLAY_SEQUENCE(agent, Hash40::new("seq_robot_rnd_ottotto"));
    }
}