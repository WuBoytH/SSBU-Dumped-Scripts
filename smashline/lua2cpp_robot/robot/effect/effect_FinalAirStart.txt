#[acmd_script( agent = "robot", script = "effect_finalairstart" , category = ACMD_EFFECT , low_priority)]
unsafe fn effect_finalairstart(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("bg_robot_final"), false);
        EffectModule::req_screen(fighter.module_accessor);
    }
}