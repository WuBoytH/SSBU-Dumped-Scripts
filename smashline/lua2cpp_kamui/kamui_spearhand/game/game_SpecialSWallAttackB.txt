#[acmd_script( agent = "kamui_spearhand", script = "game_specialswallattackb" , category = ACMD_GAME , low_priority)]
unsafe fn game_specialswallattackb(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 10.0);
    if macros::is_excute(fighter) {
        WorkModule::on_flag(fighter.module_accessor, *WEAPON_KAMUI_SPEARHAND_STATUS_WORK_FLAG_PIERCE_RELEASE);
    }
}