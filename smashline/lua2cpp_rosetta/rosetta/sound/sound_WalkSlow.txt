#[acmd_script( agent = "rosetta", script = "sound_walkslow", category = ACMD_SOUND, low_priority)]
unsafe fn sound_walkslow(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(fighter.lua_state_agent, 22.0);
    macros::PLAY_STATUS(fighter, 0x17b9aad280);
}