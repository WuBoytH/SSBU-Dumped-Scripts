#[acmd_script( agent = "buddy", script = "game_jumpfront" , category = ACMD_GAME , low_priority)]
unsafe fn game_jumpfront(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    macros::FT_MOTION_RATE(fighter, 0.25);
    frame(fighter.lua_state_agent, 13.0);
    macros::FT_MOTION_RATE(fighter, 1.75);
    frame(fighter.lua_state_agent, 25.0);
    macros::FT_MOTION_RATE(fighter, 1.25);
    frame(fighter.lua_state_agent, 29.0);
    macros::FT_MOTION_RATE(fighter, 1);
}