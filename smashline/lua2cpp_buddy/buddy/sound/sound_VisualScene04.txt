#[acmd_script( agent = "buddy", script = "sound_visualscene04", category = ACMD_SOUND, low_priority)]
unsafe fn sound_visualscene04(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 8.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_buddy_final06"));
    }
}