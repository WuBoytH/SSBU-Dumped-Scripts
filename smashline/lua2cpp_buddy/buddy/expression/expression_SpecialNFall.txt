#[acmd_script( agent = "buddy", script = "expression_specialnfall" , category = ACMD_EXPRESSION , low_priority)]
unsafe fn expression_specialnfall(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		ItemModule::set_have_item_visibility(fighter.module_accessor, false);
	}
