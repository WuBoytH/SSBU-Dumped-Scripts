#[acmd_script( agent = "buddy_bird", script = "game_visualscene01c", category = ACMD_GAME, low_priority)]
unsafe fn game_visualscene01c(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 37.0);
    if macros::is_excute(fighter) {
        VisibilityModule::set_whole(fighter.module_accessor, false);
    }
}