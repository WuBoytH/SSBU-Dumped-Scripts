#[acmd_script( agent = "popo", script = "expression_entrylnana", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_entrylnana(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
}