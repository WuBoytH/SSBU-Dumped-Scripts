#[acmd_script( agent = "popo", script = "game_lgunshootairempty_nana", category = ACMD_GAME, low_priority )]
unsafe fn game_lgunshootairempty_nana(agent: &mut L2CAgentBase) {
    frame(agent.lua_state_agent, 9.0);
    if macros::is_excute(agent) {
        macros::SHOOT_ITEM_BULLET(agent);
    }
}