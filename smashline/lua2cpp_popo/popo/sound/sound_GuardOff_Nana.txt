#[acmd_script( agent = "popo", script = "sound_guardoff_nana", category = ACMD_SOUND, low_priority )]
unsafe fn sound_guardoff_nana(agent: &mut L2CAgentBase) {
    frame(agent.lua_state_agent, 2.0);
    if macros::is_excute(agent) {
        macros::PLAY_SE(agent, Hash40::new("se_common_guardoff"));
    }
}