#[acmd_script( agent = "popo_iceshot", script = "effect_situationair", category = ACMD_EFFECT, low_priority)]
unsafe fn effect_situationair(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::EFFECT_OFF_KIND(fighter, Hash40::new("popo_iceshot_cold_b"), false, true);
    }
}