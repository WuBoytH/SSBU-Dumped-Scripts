#[acmd_script( agent = "cloud", script = "sound_specials2_lb", category = ACMD_SOUND, low_priority)]
unsafe fn sound_specials2_lb(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 2.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_cloud_special_s08"));
    }
}