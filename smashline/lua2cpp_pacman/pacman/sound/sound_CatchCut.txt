#[acmd_script( agent = "pacman", script = "sound_catchcut", category = ACMD_SOUND, low_priority )]
unsafe fn sound_catchcut(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        sound!(agent, *MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}