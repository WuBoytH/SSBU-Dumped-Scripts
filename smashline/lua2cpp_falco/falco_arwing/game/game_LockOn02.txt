#[acmd_script( agent = "falco_arwing", script = "game_lockon02" , category = ACMD_GAME , low_priority)]
unsafe fn game_lockon02(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 45.0);
    if macros::is_excute(fighter) {
        WorkModule::on_flag(fighter.module_accessor, *WEAPON_FALCO_ARWING_STATUS_WORK_FLAG_SET_DEPTH_OFFSET);
    }
}