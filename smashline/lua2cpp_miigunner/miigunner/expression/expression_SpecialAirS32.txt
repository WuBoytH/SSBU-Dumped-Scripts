#[acmd_script( agent = "miigunner", script = "expression_specialairs32", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairs32(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 23.0);
    if macros::is_excute(fighter) {
        ControlModule::set_rumble(fighter.module_accessor, Hash40::new("rbkind_attackm"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}