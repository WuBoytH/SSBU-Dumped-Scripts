#[acmd_script( agent = "miigunner", script = "sound_finalair" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_finalair(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("vc_mii_final_gunner"));
    }
}