#[acmd_script( agent = "mewtwo", script = "sound_wait1", category = ACMD_SOUND, low_priority )]
unsafe fn sound_wait1(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        sound!(fighter, *MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}