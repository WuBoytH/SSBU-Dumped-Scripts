#[acmd_script( agent = "mewtwo", script = "sound_waititem" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_waititem(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		sound(*MA_MSC_CMD_SOUND_STOP_SE_STATUS);
	}
