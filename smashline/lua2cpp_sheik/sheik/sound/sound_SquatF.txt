#[acmd_script( agent = "sheik", script = "sound_squatf" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_squatf(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(fighter.lua_state_agent, 10.0);
    if macros::is_excute(fighter) {
        macros::PLAY_STEP(fighter, Hash40::new("se_sheik_crawing_left"));
    }
    frame(fighter.lua_state_agent, 19.0);
    macros::PLAY_STEP(fighter, Hash40::new("se_sheik_crawing_right"));
}