#[acmd_script( agent = "sheik", script = "effect_finaldashend" , category = ACMD_EFFECT , low_priority)]
unsafe fn effect_finaldashend(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("bg_sheik_final"));
        EffectModule::remove_screen(fighter.module_accessor);
        macros::CANCEL_FILL_SCREEN(fighter, 0, 50);
        macros::CANCEL_FILL_SCREEN(fighter, 1, 50);
    }
    frame(fighter.lua_state_agent, 10.0);
    if macros::is_excute(fighter) {
        macros::EFFECT_OFF_KIND(fighter, Hash40::new("sheik_final_eye"), false, false);
        macros::EFFECT_OFF_KIND(fighter, Hash40::new("sheik_final_knife"), false, false);
    }
}