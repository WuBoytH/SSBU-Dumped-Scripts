#[acmd_script( agent = "sheik", script = "expression_squatf", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_squatf(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    if macros::is_excute(fighter) {
        slope(*MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_TOP, 4, true);
    }
    frame(fighter.lua_state_agent, 10.0);
    if macros::is_excute(fighter) {
        methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_walk"), 0, false);
        ControlModule::set_rumble(fighter.module_accessor);
    }
    frame(fighter.lua_state_agent, 19.0);
    methodlib::L2CValue::as_hash()const(Hash40::new("rbkind_walk"), 0, false);
    ControlModule::set_rumble(fighter.module_accessor);
}