#[acmd_script( agent = "miifighter", script = "expression_specialairlw2start", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_specialairlw2start(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 3.0);
    if macros::is_excute(fighter) {
        ControlModule::set_rumble(fighter.module_accessor, Hash40::new("rbkind_jump"), 0, false, *BATTLE_OBJECT_ID_INVALID as u32);
    }
}