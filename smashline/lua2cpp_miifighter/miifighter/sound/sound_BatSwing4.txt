#[acmd_script( agent = "miifighter", script = "sound_batswing4", category = ACMD_SOUND, low_priority)]
unsafe fn sound_batswing4(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 45.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SEQUENCE(fighter, Hash40::new("seq_miifighter_rnd_attack03"));
    }
}