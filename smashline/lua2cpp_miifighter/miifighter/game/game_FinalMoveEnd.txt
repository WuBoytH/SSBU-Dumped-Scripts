#[acmd_script( agent = "miifighter", script = "game_finalmoveend", category = ACMD_GAME, low_priority )]
unsafe fn game_finalmoveend(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::WHOLE_HIT(fighter, *HIT_STATUS_XLU);
    }
}