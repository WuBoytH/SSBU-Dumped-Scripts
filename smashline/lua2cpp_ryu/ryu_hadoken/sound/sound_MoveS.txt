#[acmd_script( agent = "ryu_hadoken", script = "sound_moves", category = ACMD_SOUND, low_priority )]
unsafe fn sound_moves(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_ryu_special_n04"));
    }
}