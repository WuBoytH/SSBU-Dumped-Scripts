#[acmd_script( agent = "miiswordsman", script = "expression_ottotto", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_ottotto(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        slope!(agent, *MA_MSC_CMD_SLOPE_SLOPE, *SLOPE_STATUS_L);
    }
}