#[acmd_script( agent = "koopag", script = "sound_specialssquat" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialssquat(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::STOP_SE(fighter, Hash40::new("se_koopag_special_s01"));
        macros::PLAY_SE(fighter, Hash40::new("se_koopag_special_s02"));
    }
    wait(fighter.lua_state_agent, 5.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, 0x0dae3a633b);
    }
}