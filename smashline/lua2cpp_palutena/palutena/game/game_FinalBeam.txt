#[acmd_script( agent = "palutena", script = "game_finalbeam" , category = ACMD_GAME , low_priority)]
unsafe fn game_finalbeam(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		camera(*MA_MSC_CMD_CAMERA_CAM_RECT, 25, -10, 10, -15);
	}
