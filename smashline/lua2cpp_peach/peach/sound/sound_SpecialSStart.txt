#[acmd_script( agent = "peach", script = "sound_specialsstart", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialsstart(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 1.0);
    if macros::is_excute(fighter) {
        macros::PLAY_STATUS(fighter, Hash40::new("vc_peach_002"));
    }
    wait(fighter.lua_state_agent, 5.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_peach_special_s03"));
    }
}