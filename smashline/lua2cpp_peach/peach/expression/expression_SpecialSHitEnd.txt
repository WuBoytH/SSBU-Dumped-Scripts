#[acmd_script( agent = "peach", script = "expression_specialshitend", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_specialshitend(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 2.0);
    if macros::is_excute(fighter) {
        macros::QUAKE(fighter, *CAMERA_QUAKE_KIND_L);
        macros::RUMBLE_HIT(fighter, Hash40::new("rbkind_beamm"), 0);
    }
}