#[acmd_script( agent = "inkling", script = "sound_specialinkchargestart" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialinkchargestart(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 5.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_inkling_inkchargestart"));
    }
}