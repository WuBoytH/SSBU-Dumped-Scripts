#[acmd_script( agent = "inkling", script = "sound_final", category = ACMD_SOUND, low_priority )]
unsafe fn sound_final(agent: &mut L2CAgentBase) {
    frame(agent.lua_state_agent, 3.0);
    if macros::is_excute(agent) {
        macros::PLAY_SE(agent, Hash40::new("vc_inkling_final01"));
    }
}