#[acmd_script( agent = "inkling", script = "expression_attackdash_cancel", category = ACMD_EXPRESSION, low_priority)]
unsafe fn expression_attackdash_cancel(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::EFFECT_OFF_KIND(fighter, Hash40::new("inkling_squid_change"), true, true);
    }
}