#[acmd_script( agent = "inkling", script = "game_specialsrunturn" , category = ACMD_GAME , low_priority)]
unsafe fn game_specialsrunturn(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        FighterAreaModuleImpl::enable_fix_jostle_area(fighter.module_accessor, 3, 8);
    }
    frame(fighter.lua_state_agent, 18.0);
    if macros::is_excute(fighter) {
        WorkModule::on_flag(fighter.module_accessor, *FIGHTER_INKLING_STATUS_SPECIAL_S_FLAG_TURN);
    }
}