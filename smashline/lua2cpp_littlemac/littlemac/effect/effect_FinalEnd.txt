#[acmd_script( agent = "littlemac", script = "effect_finalend" , category = ACMD_EFFECT , low_priority)]
unsafe fn effect_finalend(fighter: &mut L2CAgentBase) {
	if macros::is_excute(fighter) {
		macros::FT_REMOVE_FINAL_AURA(fighter, true);
	}
