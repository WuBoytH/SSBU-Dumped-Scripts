#[acmd_script( agent = "brave", script = "sound_sleeploop", category = ACMD_SOUND, low_priority )]
unsafe fn sound_sleeploop(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_STATUS(fighter, Hash40::new("vc_brave_furasleep"));
    }
    wait(fighter.lua_state_agent, 160.0);
}