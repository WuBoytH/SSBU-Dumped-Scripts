#[acmd_script( agent = "marth", script = "sound_squat", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squat(agent: &mut L2CAgentBase) {
    frame(agent.lua_state_agent, 3.0);
    if macros::is_excute(agent) {
        macros::PLAY_SE(agent, Hash40::new("se_marth_squat"));
    }
}