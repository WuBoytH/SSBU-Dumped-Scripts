#[acmd_script( agent = "dedede", script = "sound_specialairsmiss", category = ACMD_SOUND, low_priority)]
unsafe fn sound_specialairsmiss(fighter: &mut L2CAgentBase) {
    frame(fighter.lua_state_agent, 26.0);
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_dedede_hammer_swing_m"));
    }
}