#[acmd_script( agent = "dedede", script = "sound_specialsget" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialsget(fighter: &mut L2CAgentBase) {
	frame(fighter.lua_state_agent, 16.0);
	if macros::is_excute(fighter) {
		macros::PLAY_SE(fighter, Hash40::new("se_dedede_wad_throw"));
	}
