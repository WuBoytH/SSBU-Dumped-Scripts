#[acmd_script( agent = "dedede", script = "effect_finalend", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_finalend(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::EFFECT_OFF_KIND(fighter, Hash40::new("dedede_final_damage"), true, true);
        macros::EFFECT_STENCIL_OFF(fighter);
    }
}