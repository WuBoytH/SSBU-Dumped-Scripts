#[acmd_script( agent = "pikachu", script = "sound_specialairsmissend" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_specialairsmissend(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        sound(*MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}