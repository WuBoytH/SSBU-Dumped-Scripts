#[acmd_script( agent = "snake", script = "expression_appealwait", category = ACMD_EXPRESSION, low_priority )]
unsafe fn expression_appealwait(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        slope!(fighter, *MA_MSC_CMD_SLOPE_SLOPE_INTP, *SLOPE_STATUS_TOP, 3, true);
        ItemModule::set_have_item_visibility(fighter.module_accessor, false, 0);
    }
}