#[acmd_script( agent = "link", script = "sound_linkbowchargemax", category = ACMD_SOUND, low_priority )]
unsafe fn sound_linkbowchargemax(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::PLAY_SE(fighter, Hash40::new("se_link_special_n04"));
    }
}