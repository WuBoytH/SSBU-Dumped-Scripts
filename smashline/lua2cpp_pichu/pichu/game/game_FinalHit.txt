#[acmd_script( agent = "pichu", script = "game_finalhit", category = ACMD_GAME, low_priority)]
unsafe fn game_finalhit(fighter: &mut L2CAgentBase) {
    if macros::is_excute(fighter) {
        macros::WHOLE_HIT(fighter, *HIT_STATUS_XLU);
    }
}