#[acmd_script( agent = "pichu", script = "game_finalreturn", category = ACMD_GAME, low_priority )]
unsafe fn game_finalreturn(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        macros::WHOLE_HIT(agent, *HIT_STATUS_XLU);
    }
}