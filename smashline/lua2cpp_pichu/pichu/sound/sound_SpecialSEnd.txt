#[acmd_script( agent = "pichu", script = "sound_specialsend", category = ACMD_SOUND, low_priority )]
unsafe fn sound_specialsend(agent: &mut L2CAgentBase) {
    if macros::is_excute(agent) {
        sound!(agent, *MA_MSC_CMD_SOUND_STOP_SE_STATUS);
    }
}