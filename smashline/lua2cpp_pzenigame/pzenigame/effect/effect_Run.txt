#[acmd_script( agent = "pzenigame", script = "effect_run", category = ACMD_EFFECT, low_priority )]
unsafe fn effect_run(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    if macros::is_excute(fighter) {
        macros::EFFECT_FOLLOW(fighter, Hash40::new("pzenigame_run"), Hash40::new("top"), 0, 0, 0, 0, 0, 0, 1.4, true);
        EffectModule::enable_sync_init_pos_last(fighter.module_accessor);
    }
    for(4 Iterations){
    wait(fighter.lua_state_agent, 8.0);
    if macros::is_excute(fighter) {
        macros::EFFECT_FOLLOW(fighter, Hash40::new("pzenigame_run"), Hash40::new("top"), 0, 0, 1.5, 0, 0, 0, 1, true);
        EffectModule::enable_sync_init_pos_last(fighter.module_accessor);
    }
}
}