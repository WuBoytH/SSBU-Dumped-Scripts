#[acmd_script( agent = "wiifit", script = "sound_squatb", category = ACMD_SOUND, low_priority )]
unsafe fn sound_squatb(fighter: &mut L2CAgentBase) {
    wait_loop_sync_mot();
    frame(fighter.lua_state_agent, 22.0);
    if macros::is_excute(fighter) {
        macros::PLAY_STEP(fighter, Hash40::new("se_wiifit_crawing_right_b"));
    }
    wait(fighter.lua_state_agent, 40.0);
    macros::PLAY_STEP(fighter, 0x1873b6f18b);
}