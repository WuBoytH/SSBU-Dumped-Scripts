#[acmd_script( agent = "kirby", script = "sound_sonicspecialnstart" , category = ACMD_SOUND , low_priority)]
unsafe fn sound_sonicspecialnstart(fighter: &mut L2CAgentBase) {
	frame(fighter.lua_state_agent, 3.0);
	if macros::is_excute(fighter) {
		macros::PLAY_STATUS(fighter, Hash40::new("se_sonic_roundholdloop"));
	}
